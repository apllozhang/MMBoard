# MMBoard 第三轮独立整改复审报告

> 复审日期：2026-09-22  
> 复审依据：`MMBOARD_SECOND_REASSESSMENT_2026-09-22.md`、`MMBOARD_交付复审_二轮_2026-09-22.md`  
> 功能代码提交：`b48d4cf`  
> 当前 GitHub `main`：`fe0a6f2`（仅增加交付文档）  
> 生产版本：`b48d4cf`

## 1. 总体结论

本轮整改具有可验证的实质进展。复审实际运行了密钥扫描、三套自动化测试、类型检查和生产构建，结果均通过。第二轮发现的五个重点问题中，R02 编号高水位、speaker 二次改名、候选数据隔离、非 demo LLM 静默 mock、ASR 设置并发控制均已得到有效修复。

本轮 R01–R25 结论：

- **PASS：15 项**：R01、R02、R03、R04、R06、R07、R08、R10、R11、R14、R16、R20、R21、R22、R25。
- **PARTIAL：10 项**：R05、R09、R12、R13、R15、R17、R18、R19、R23、R24。
- **FAIL：0 项**。

项目已经从“存在阻断性整改缺陷”进入“核心风险基本关闭、剩余改进范围明确”的状态，但不应描述为 R01–R25 全部完成。凭据轮换仍是必须由用户执行的外部操作。

## 2. 版本核对

| 对象 | 复审实测 | 结论 |
|---|---|---|
| 本地 HEAD | `fe0a6f27d7e452ac621f126063dc21248c8e528d` | 仅文档提交位于功能提交之后 |
| GitHub `main` | `fe0a6f27d7e452ac621f126063dc21248c8e528d` | `git ls-remote` 实测 |
| 生产 `/api/version` | `b48d4cf` | 功能代码提交 |
| `b48d4cf..HEAD` 指定代码目录 diff | 空 | `server/`、`src/`、`tools/`、`scripts/`、`deploy/` 与生产功能提交一致 |

开发方本轮没有再声称三方 SHA 完全相同，而是区分了功能提交、文档提交和生产镜像，口径可以接受。

## 3. 实际验证记录

| 验证命令 | 复审结果 |
|---|---|
| `npm run scan:secrets` | 通过：扫描 273 个跟踪文件，按本机 4 个真实凭据值比对，未发现泄漏 |
| `node scripts/verify_batch_a.cjs` | 94/94 通过 |
| `python scripts/regression_batch1.py` | 30/30 通过 |
| `python tools/local-asr/test_server.py` | 11/11 通过；控制台中文输出存在编码乱码，不影响断言结果 |
| `npm run typecheck` | 通过 |
| `npm run build` | 通过；仍有 `./fonts/noto.css` 不存在警告 |
| 生产 `/api/version` | 返回 `b48d4cf` |
| `.bak` 状态 | 文件已不存在，且 `.gitignore` 能匹配 `server/meeting.secret.json.bak` |

`scripts/regression_batch1.py` 仍会把测试会话 token 的大部分内容打印到控制台。它使用隔离账号和临时环境，当前不构成生产泄漏，但测试日志应避免输出认证令牌。

## 4. 第二轮重点问题复核

### 4.1 R02 删除最新编号后复用：已修复

`server/pipeline.cjs:164-185` 增加独立的 `seq-highwater.json`。删除当天最大编号后，高水位不会随任务和 UUID 输出目录一起消失。

运行期证据：

- API 回归覆盖“创建 → 删除当天最新编号 → 重启 → 再创建”。
- 并发创建 6 条任务均成功且编号唯一。
- `R02_deletedLatestNotReused`、`R02_uniqueAfterRestart` 均通过。

**裁定：R02 PASS。**

限制：`loadSeqHighwater()` 捕获包括主备同时损坏在内的所有错误并返回空对象。若高水位主备文件同时损坏，且最新任务已删除，仍可能重新使用编号。该问题归入 R09 的损坏恢复边界，不推翻正常运行和普通重启下的 R02 结论。

### 4.2 speaker 二次改名：已修复

- “重新跑分析”不再把人工姓名写入 transcript。
- LLM 始终接收 `说话人N` canonical 文本。
- `analysis.json` 和统计数据保存 canonical 值，姓名只在渲染层映射。
- stub LLM 端到端测试覆盖“改名甲 → 重跑分析 → 改名乙 → 纯重渲染”，最终纪要包含乙且甲零残留。

**裁定：相关新增 P1 缺陷关闭，R16 维持 PASS。**

### 4.3 候选容器写生产卷：核心问题已修复

`deploy/deploy.cjs:139-159` 为候选创建独立 Docker volume，并将生产数据复制到候选卷。候选启动后的恢复和状态写入不再触碰正式数据卷。正式切换和回滚均使用不可变 image ID。

开发方声明已经在生产执行 `DEPLOY_DRILL=1` 并恢复旧版本；复审没有再次执行生产写操作，也没有在仓库中找到独立部署日志，因此该演练证据来自交付记录，不是本轮重复实测。当前部署代码路径与生产版本均支持该陈述。

**裁定：R11 PASS。**

改进建议：演练前先记录旧 commit，并在回滚后精确比较该 commit；当前脚本只要求回滚后存在 commit 且不等于新 commit。候选快照复制生产正在写入的数据时也可能得到时间点不一致的副本，虽然不会反向损坏生产，但可能产生无意义的候选失败。

### 4.4 非 demo LLM 静默 mock：已修复

`server/llm.cjs:88-103` 使用统一 `hasUsableConfig`：

- 显式 demo 才返回 mock。
- 非 demo 缺少 `baseUrl/apiKey/model` 时明确失败。
- meta、重跑预览和执行路径使用相同配置判定。

stub 运行期断言通过。

**裁定：R06 PASS。**

### 4.5 ASR 设置并发控制：已修复

`PUT /api/settings/asr` 当前行为：

- 缺少 version：400。
- version 过期：409。
- version 匹配：保存成功并递增版本。

前端快捷切换会携带当前版本并处理错误。运行期断言覆盖完整 400/409/200 路径。

**裁定：R22 PASS。**

## 5. R01–R25 最终矩阵

| 项 | 结论 | 复审说明 |
|---|---|---|
| R01 | PASS | 同名上传、存储隔离、删除和重跑有端到端证据。 |
| R02 | PASS | UUID 目录键与持久化高水位共同阻止正常运行下的身份复用；删除最新项和重启场景已覆盖。 |
| R03 | PASS | fail-closed、随机初始密码、首次原子写和 0600、登录限流、审计清洗、撤销一致均有证据；单管理员是当前产品边界。 |
| R04 | PASS | 受保护产物接口有效，旧静态输出路径关闭。 |
| R05 | PARTIAL | LLM 默认拒私网、测试限流生效；`allowPrivate` 仍是全局开关，DNS 校验与连接分离存在 rebinding 窗口，本地 ASR 重定向未逐跳复验。 |
| R06 | PASS | 转写与分析非 demo 缺配置均明确失败，模拟模式显式且持续标记。 |
| R07 | PASS | 运行互斥与 runId 归属成立。 |
| R08 | PASS | 全局队列、入队前检查和中断恢复成立。 |
| R09 | PARTIAL | 主要状态已统一原子持久化；`analysis.json` 等仍直接写，固定 `.tmp` 不支持多进程，高水位与额度读取仍有捕获全部错误后回零的路径。 |
| R10 | PASS | 部署依赖和版本接口完整。 |
| R11 | PASS | 不可变镜像、隔离候选卷和回滚路径已落实；生产演练未由本轮重复执行。 |
| R12 | PARTIAL | `.bak` 已删除并忽略，扫描脚本通过，首次 auth 权限已补；外部凭据尚未轮换，镜像层扫描未成为强制 CI。 |
| R13 | PARTIAL | 维持上轮裁定：均匀采样是有效缓解，不能保证采样点外唯一决议被提取。 |
| R14 | PASS | 缺失、类型错误和截断结果会标记 partial，不再伪装为“无”。 |
| R15 | PARTIAL | 三类日期已分离；行动项 `tref/quote` 与 transcript segment 尚未做真实性强校验。 |
| R16 | PASS | speaker 各输出上下文转义和二次改名均有运行证据。 |
| R17 | PARTIAL | 429、503、超大响应和正常路径已有 provider stub；讯飞轮询瞬态重试仍未实现。 |
| R18 | PARTIAL | 生命周期和限流改善；媒体内容探测、流式限速、磁盘水位与总存储配额未完成。 |
| R19 | PARTIAL | stub 模型已驱动真实 HTTP、worker、队列、TTL 和失败路径；真实 FunASR 模型环境仍未验证性能与稳定性。 |
| R20 | PASS | 详情实时派生和防重叠轮询成立。 |
| R21 | PASS | 草稿保留、异常提示和 meta 刷新成立。 |
| R22 | PASS | 主设置及 ASR 快捷写路径均具备当前客户端的 version 冲突保护。 |
| R23 | PARTIAL | inert、aria-hidden 和顶层焦点逻辑已实现；320px、低高度、键盘和读屏浏览器矩阵仍无完成证据。 |
| R24 | PARTIAL | 三套脚本共 135 项断言并有较多运行期覆盖；浏览器矩阵、真实 FunASR、部分部署证据和完整 provider 故障矩阵仍缺。 |
| R25 | PASS | 三档性能基线已冻结并可重复比较。 |

## 6. 密钥扫描和凭据轮换裁定

本轮确认：

- `server/meeting.secret.json.bak` 已不存在。
- `.gitignore` 对主文件和 `.bak` 均生效。
- `npm run scan:secrets` 在当前开发机读取 4 个真实凭据值，并确认这些值未出现在 Git 跟踪文件中。

扫描器仍有两个边界：

1. 全新 CI 没有本地真实密钥文件时，值匹配集合为空，只能检查敏感文件名，不能识别未知格式的新密钥。
2. `tar` 不可用或归档读取失败时，脚本会静默跳过归档检查并继续成功退出。

因此 R12 仍为 PARTIAL。建议引入通用 secret scanner，并让归档检查失败时 fail-closed。

曾出现在 `.bak` 中的讯飞、LLM 及相关部署凭据应视为可能泄露。删除文件不会使旧凭据失效；用户仍需在相应外部系统完成轮换，并在 MMBoard 设置页更新。复审没有执行外部凭据轮换。

## 7. 后续工作建议

按风险和收益排序：

1. 用户完成讯飞、LLM 和部署凭据轮换。
2. R05 将私网放行改成明确主机/端口列表，并绑定校验后的 IP 发起连接，关闭 DNS rebinding 窗口。
3. R09 让高水位和额度文件主备同时损坏时明确失败，analysis JSON 也走统一持久化。
4. R17 为讯飞轮询增加有限次数、分类错误和退避策略。
5. R23 在稳定浏览器环境完成窄屏、低高度、键盘、焦点和读屏矩阵。
6. R19 在真实 FunASR 模型与代表性长音频下记录健康响应、排队、内存、转码和 TTL。
7. R13/R15/R18 按产品优先级继续补全文覆盖、证据强绑定和资源治理。

本轮可以关闭第二轮报告列出的五项核心整改缺陷，但尚不能将十项 PARTIAL 标记为完成。

