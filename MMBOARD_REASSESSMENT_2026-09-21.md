# MMBoard 独立整改复审报告

> 复审日期：2026-09-21  
> 复审角色：独立评审方，只做验证和提出整改要求，不参与代码建设。  
> 原报告：`CODE_REVIEW.md`（R01–R25）  
> 复审代码：本地 `main` / `f83dfe1`  
> 生产站点：`http://10.20.30.203:8099/`  
> 结论定义：`PASS` = 原验收标准已成立；`PARTIAL` = 有有效整改，但原验收标准未全部成立；`FAIL` = 关键验收目标未成立或出现等价高风险缺陷。

## 1. 复审结论

本次不能确认“R01–R23 已全部整改完成”。按原报告的验收标准，R01–R23 中：

- **PASS：5 项**：R01、R04、R07、R10、R20。
- **PARTIAL：13 项**：R02、R03、R06、R08、R09、R12、R13、R14、R17、R18、R21、R22、R23。
- **FAIL：5 项**：R05、R11、R15、R16、R19。
- 持续项：R24 `FAIL`，R25 `FAIL`。

整改确实解决了一部分原问题，但当前仍有阻断上线验收的缺陷：认证配置损坏后恢复公开默认密码、部署回滚实际仍可能启动失败镜像、说话人名称可触发纪要页面 DOM XSS、讯飞配置字段契约断裂而静默使用模拟转写、队列满时重跑任务永久卡在 `queued`、说话人重命名后纪要正文与统计可能出现不同姓名。

## 2. 版本与验收范围

### 2.1 三个环境不是同一版本

| 检查对象 | 实际提交 | 结论 |
|---|---:|---|
| 本地工作区 `main` | `f83dfe1` | 本报告的静态代码复审对象 |
| GitHub `main` | `55eab80` | 只读执行 `git ls-remote origin refs/heads/main`；缺少本地的 `f83dfe1` |
| 生产 `/api/version` | `2a45acf` | 不是整改提交链的最终版本，不能证明后续整改已部署 |

生产接口同时返回 `builtAt=2026-09-20T23:36:21.163Z`。因此，生产验证只能证明 `2a45acf` 的行为，不能用于确认 `6c2592e`、`ba260f2`、`29463bd`、`55eab80`、`f83dfe1` 的整改效果。应先建立唯一发布版本，再重新进行生产验收。

### 2.2 已执行的验证

- `npm run typecheck`：通过。
- `npm run build`：通过；仍有 `./fonts/noto.css` 构建期未找到警告。
- Node 语法检查、Python 编译检查、`git diff --check`：通过。
- 生产匿名访问 `/api/tasks`：HTTP 401。
- 生产匿名访问已有任务纪要：HTTP 401。
- 生产旧路径 `/outputs/probe/transcript.json`：HTTP 404。
- 使用提供的凭据登录生产：成功；凭据未写入本报告。
- 生产任务列表只读查询：成功，共返回 7 条任务。
- 未向生产上传测试文件，未修改生产配置、任务或数据库。

构建和类型检查只证明代码可以构建，不能代替业务、安全、故障和并发验收。

### 2.3 回归脚本不可作为当前验收证据

`scripts/regression_batch1.py` 当前不可复现：

1. `ROOT` 被计算为 `scripts/`，随后拼出不存在的 `scripts/server/server.cjs`（第 22–25、64–66 行）。
2. 脚本没有登录、Cookie 或 `X-Requested-With`，与 R03 后的认证协议不兼容（第 32–51 行）。
3. 启动探测只要目标端口上已有服务能够响应就直接返回，没有检查新进程是否已退出，也没有验证被测版本，因此可能误连其他实例（第 64–71 行）。
4. 脚本说明写“删除中间记录不复用 ID”，实际断言允许回收已删除的 `002`，测试意图与断言不一致（第 142–153 行）。

复审过程中该脚本曾被执行，首次上传返回 503。它删除并重建了未纳入 Git 的 `_isolated_test` 目录；真实密钥由脚本 `finally` 恢复，未调用生产写接口，但原 `_isolated_test` 内容无法恢复。该副作用在此如实披露。

## 3. R01–R23 逐项复审

### R01：同名上传隔离 — PASS

**成立证据**

- `server/server.cjs:142-159` 在 Multer 顶层配置 `fileFilter`，并生成唯一存储名。
- `server/server.cjs:431-457`、`server/pipeline.cjs:171-215` 分离保存 `originalFileName` 与实际存储键。
- `server/server.cjs:641-660` 删除时按当前任务记录的文件键清理，不再按原始文件名共享删除。

**限制**

仓库中的自动化脚本当前不可运行，因此 PASS 主要来自代码路径验证，仍应补一条可复现的双同名文件端到端测试。

### R02：任务编号唯一性 — PARTIAL

**已成立**

- `server/pipeline.cjs:171-214` 同步扫描现存任务和输出目录，并生成 UUID；单 Node 进程内创建时能够避开正在占用的展示编号和残留输出目录。

**未成立**

- UUID 没有成为 API、查找和产物目录的真实主键；主要路径仍以 `MT-日期-序号` 为主键，例如 `server/server.cjs:426-428`。
- 删除后会回收空号。虽然当前单实例同步代码可避免同一进程内同时重复，但没有数据库唯一约束、文件锁或跨进程原子分配，不能通过原验收中的“并发创建和服务重启后仍不重复”。
- 提交的回归脚本与原验收口径冲突，并且不可运行。

**整改要求**

将 UUID 作为不可复用的内部主键和目录键，展示编号仅作为可读字段；至少增加多请求并发创建、删除中间/末尾任务、残留目录、重启后创建的自动化测试。

### R03：认证与授权 — PARTIAL

**已成立**

- `server/server.cjs:178-197` 对登录和状态接口以外的 `/api` 统一校验 HMAC 会话，并对非 GET/HEAD 请求检查 CSRF 请求头。
- Cookie 设置了 `HttpOnly` 和 `SameSite=Strict`；改密会轮换会话密钥（`server/server.cjs:206-244`）。
- 生产匿名访问任务列表和纪要均返回 401。

**未成立及新增风险**

- `server/auth.cjs:13-14` 将默认账号密码写在仓库中；`server/auth.cjs:22-40` 在 `auth.json` 不存在或解析失败时静默恢复该公开密码。这是 fail-open：配置损坏即可重新获得默认管理员入口。
- 没有强制首次改密，登录端点也没有失败限流或延迟。
- 只有一个全权管理员，没有普通用户或任务归属模型，无法验证“普通用户不能改设置”和对象级越权。
- `/api/auth/status` 只验证签名，不检查已撤销 token（`server/server.cjs:200-204`），可出现状态接口称已登录、业务接口返回 401 的循环。
- 审计日志 detail 未清理换行、回车和制表符；失败登录用户名可伪造审计行。`X-Forwarded-For` 也不应在未配置可信代理时直接信任。

**整改要求**

认证配置损坏时必须拒绝启动或从经过校验的备份恢复；删除公开默认密码，首次初始化使用一次性随机密码或环境注入；增加登录限流；审计日志改为结构化 JSONL 并清洗控制字符；统一 status 与中间件的撤销校验。

### R04：产物访问保护 — PASS

**成立证据**

- 旧 `/outputs` 静态挂载已移除。
- `server/server.cjs:665-674` 的纪要接口和 `server/server.cjs:628-639` 的下载接口位于认证中间件后。
- 原始 `transcript.json` 没有公开路由。
- 生产匿名访问任务纪要为 401，旧 `/outputs` 探测为 404。

**改进建议**

敏感下载和纪要响应增加 `Cache-Control: private, no-store`，降低共享浏览器或代理缓存泄漏风险。

### R05：SSRF、密钥换主机与测试接口限制 — FAIL

**已成立**

- `server/server.cjs:285-308` 在 base URL 改变时拒绝沿用打码后的旧密钥。

**关键未成立**

- LLM `baseUrl` 没有协议、主机或端口允许规则；保存后 `server/llm.cjs:14-30,68` 会请求任意地址。
- `/api/settings/test` 直接请求用户提供的地址且没有限流（`server/server.cjs:336-378`）。
- 本地 ASR 只用正则检查初始私网 URL（`server/server.cjs:52-54,391-421`）；`fetch` 默认跟随重定向，`server/local.cjs:22,39,56` 没有再次验证跳转目的。
- 原验收中的“不允许地址被拒绝”和“重定向后目的校验”没有成立。

**整改要求**

对 LLM 和 ASR 分别建立协议、主机、端口白名单；解析 DNS 后拒绝不符合策略的地址；禁用自动重定向或逐跳复验；对测试接口、登录和上传分别限流；限制响应大小和总耗时。

### R06：模拟模式标识 — PARTIAL

**已成立**

- `server/pipeline.cjs:285-307,347-358` 保存转写/分析模式并传给模板。
- `server/minutes-template.cjs:25-34,184-187` 在纪要中持续显示模拟警告，不只使用短暂 toast。

**未成立**

- `server/iflytek.cjs:87-91` 在生产缺密钥时仍自动退回 mock，没有显式 demo/production 开关；生产误配置可能静默生成模拟内容。
- `transcript.json` 本身没有保存 mode，只依赖任务与分析元数据，证据链不完整。
- 更严重的是新增的讯飞字段契约断裂，见第 4.1 节：界面可显示已配置，适配器却静默进入 mock。

**整改要求**

生产模式缺配置必须明确失败；只有显式测试模式才允许 mock。转写、分析和最终产物分别持久化 mode、provider、模型、时间和运行 ID，并补四种组合的自动化验收。

### R07：运行实例互斥与归属 — PASS

**成立证据**

- `server/server.cjs:493-531` 对运行中任务再次重跑返回 409，并生成新 `runId`。
- `server/server.cjs:644-650` 拒绝删除运行中任务。
- `server/pipeline.cjs:60-66,94-105,166-169,283,333,337` 在异步阶段检查运行归属，旧执行不能覆盖新运行结果。

**限制**

此结论针对单进程模型；多实例部署仍需要共享锁或持久化状态机。

### R08：全局队列与重启恢复 — PARTIAL

**已成立**

- `server/pipeline.cjs:108-130` 实现全局串行队列和容量限制。
- `server/pipeline.cjs:132-154` 在启动时把中断的运行/排队任务明确标为失败，不再永久显示运行中。

**新增缺陷**

- 重跑接口先把任务写成 `queued`（`server/server.cjs:520-524`），随后才调用 `enqueue`。队列已满时返回 503（第 526–529 行）却不回滚，任务会永久卡在 `queued`，之后重跑和删除都可能被 409 拒绝。
- 上传满队列后的回滚又直接写 `tasks.json`，绕过 R09 的原子写路径。

**整改要求**

入队成功与状态持久化必须成为一个可回滚事务；失败时恢复原任务状态。增加“队列正好满”“并发重跑”“服务在每个阶段重启”的自动化测试。

### R09：原子持久化与损坏恢复 — PARTIAL

**已成立**

- `server/pipeline.cjs:35-56,69-76` 为流水线任务和额度文件增加临时文件、rename 和 `.bak` 回退。

**未成立**

- `server/server.cjs:454,524,599,602,623,653` 等 API 路径仍直接 `writeFileSync` 覆盖 `tasks.json`。
- `server/server.cjs:68-75` 的设置文件仍直接覆盖；解析异常直接返回空配置，违反“配置不会静默清空”。
- `server/auth.cjs:22-43` 的认证配置也直接写入，损坏时恢复公开默认账号，风险更高。
- `loadTasks` 从 `.bak` 回退后没有修复主文件；下一次保存会先把损坏主文件复制到 `.bak`，覆盖最后一份好备份。

**整改要求**

建立唯一持久化模块，任务、设置、认证、额度全部通过同一原子写和版本校验路径；回退成功后先隔离损坏文件并修复主文件；模拟写失败、截断、无权限、断电和主/备同时损坏。

### R10：部署清单与版本接口 — PASS

**成立证据**

- `deploy/Dockerfile:12-15` 和 `deploy/deploy.cjs:68-75` 已纳入 `local.cjs`、`auth.cjs` 和 vendor 图表资源。
- `server/server.cjs:164-175` 提供不含密钥的 `/api/version`；Docker 构建注入提交号和构建时间（`deploy/Dockerfile:16-19`）。

**限制**

部署脚本要求开发机预先存在被忽略的 `dist/` 和 `server/node_modules/`，自身不执行前端构建和 `npm ci`。应在全新 clone 的 CI 环境证明可构建，避免“本机已有产物”掩盖清单缺失。

### R11：候选发布、健康检查与回滚 — FAIL

**关键缺陷**

- 候选始终构建为可变标签 `ale-symphony-console:cand`（`deploy/deploy.cjs:60,128-134`）。旧生产容器的 `Config.Image` 记录的也只是该标签（第 149–151 行）；新 build 已把该标签指向新镜像，所以失败回滚再次运行 `${oldImage}`（第 155–166 行）仍可能启动新失败镜像。
- 候选健康检查把两个 HTTP 状态拼接后只检查 `includes("200")`（第 141–146 行）。`200 + 500` 仍会通过，且 curl 没有 `-f`。
- 候选没有挂载生产数据卷，不能验证真实配置、认证文件和数据兼容性；切换后的生产检查也只覆盖版本接口。
- 解包到既有远端目录而不清理，旧残留文件可能掩盖本次归档缺文件。

**整改要求**

以不可变 digest 或提交 SHA 保存旧镜像和候选镜像；两个健康接口分别要求 2xx 和正确响应体；候选使用只读快照验证真实数据兼容；发布演练必须证明失败后运行的镜像 digest 与发布前完全一致。

### R12：密钥、传输与容器权限 — PARTIAL

**已成立**

- 真实 secret 已从部署归档和 Docker COPY 清单移除；运行时优先读取数据卷（`server/server.cjs:38-42`）。
- 按用户书面决策，本次不因纯内网 HTTP、容器以 root 运行或未设置 CPU/内存限制扣分。

**未成立**

- `settings.json` 和 `auth.json` 保存密钥时使用普通 `writeFileSync`，没有显式最小文件权限，也没有原子保护（`server/server.cjs:72-75`、`server/auth.cjs:38,43`）。
- 尚无镜像层、构建上下文和发布归档的自动化 secret 扫描证据。

**整改要求**

密钥文件创建时显式设置仅服务账号可读写的权限；部署 CI 对归档、镜像层和仓库执行 secret 扫描；记录密钥轮换流程。

### R13：长会议采样与预算 — PARTIAL

**已成立**

- `server/llm.cjs:159-171` 修复换行搜索限距和切片返回值问题，并对输入设置硬预算。
- 模板会展示采样提示；超长单行不会再无限突破预算。

**未成立**

- 算法仍只保留头尾并直接丢弃中段（`server/llm.cjs:169`）。当唯一关键决议只在中段时必然无法提取，原核心验收未通过。

**整改要求**

使用分块摘要/Map-Reduce 或按时间均匀采样，并保留片段 ID、时间范围和覆盖率；增加“唯一决议只在中段”的固定回归用例。

### R14：LLM 输出结构校验 — PARTIAL

**已成立**

- `server/llm.cjs:121-153` 规范化字段，错误类型和 `null` 不再直接导致渲染崩溃。
- finish reason 和截断修复能够把部分结果标为 partial，模板会显示提示。

**未成立**

- 语法有效但字段残缺的对象，例如只有 `title` 或 `topics` 类型错误，会被补成空数组，但 `partial=false`。
- `server/minutes-template.cjs:381,388,398` 仍把这些缺失字段显示成“无”，会把残缺结果伪装成已确认没有内容。

**整改要求**

建立严格 schema，区分“模型明确返回空”“字段缺失”“字段类型错误”和“输出截断”；缺少必需字段必须标 partial 或失败，不能渲染成“无”。

### R15：日期语义与原文追溯 — FAIL

**已改但无效**

- 模板文案从“会议日期”改为“生成日期”（`server/minutes-template.cjs:365`）。

**关键未成立**

- `server/pipeline.cjs:347,356` 传入的仍是 `task.createdAt`，即上传/任务创建时间。重跑后的“生成日期”仍显示原上传日期，字段只是改名，没有修正语义。
- 行动项 quote 只存在于提示词要求，没有来源片段 ID、时间范围或强校验，无法保证可追溯。
- 未确认人名没有系统化置信度或待确认标识。

**整改要求**

分别持久化 `meetingOccurredAt`（可空/待确认）、`uploadedAt`、`generatedAt`；行动项绑定 transcript segment ID 和时间范围；无法追溯时明确标为模型推断。

### R16：speaker HTML 转义 — FAIL

**已成立部分**

- 摘要和表格中的 speaker 使用了 `esc`（`server/minutes-template.cjs:153,160`）。

**仍可利用的缺陷**

- ECharts tooltip formatter 原样拼接 `x.speaker`（`server/minutes-template.cjs:457-460`）。JSON 源码中的 `\u003c` 在运行时会恢复为 `<`；新增手动标注允许 30 字符自由文本（`server/server.cjs:583-590`）。形如 `<img src=x onerror=...>` 的姓名可在悬停图表时进入 HTML tooltip，违反“只作为普通文字显示、不执行脚本”的原验收。

**整改要求**

tooltip 使用预先 HTML 编码的展示字段，或改用纯文本渲染；对所有 HTML、属性、脚本上下文分别做正确编码，并以恶意 speaker 回归用例验证正文、表格、图表和下载文件。

### R17：外部服务超时、中断与响应边界 — PARTIAL

**已成立**

- 讯飞单请求、本地轮询和 GLM 空正文处理已增加部分超时、容错和重试。

**未成立**

- `server/llm.cjs:14-31` 只有 socket 空闲超时，没有全请求 deadline、响应大小上限以及完整的 `aborted/error` 处理。
- 讯飞轮询的瞬态错误仍可立即使任务失败。
- LLM 对包括 400/401 在内的确定性错误也统一重试，增加重复成本且无助于恢复。
- 没有覆盖断流、慢响应、429、503、单次轮询超时的可复现故障测试。

**整改要求**

统一外部调用器：连接/首字节/总截止、最大响应体、取消信号、按错误类别重试、指数退避与 jitter；仅对可恢复错误重试并记录 attempt。

### R18：上传生命周期与资源控制 — PARTIAL

**已成立**

- `server/server.cjs:142-160` 的 `fileFilter` 位置正确，并设置 2GB 上限。
- 创建任务失败、限流和队列拒绝后会清理已落盘文件；删除会清理中间产物。

**未成立**

- 只按扩展名判断，不检查容器格式或是否存在有效音轨。
- 上传限流发生在完整文件落盘之后，攻击者仍可消耗磁盘和带宽。
- `server/iflytek.cjs:93` 和 `server/local.cjs:32` 仍把完整音频读入内存。
- 没有总存储配额、磁盘余量阈值和并发上传内存基线。

**整改要求**

在代理/流式层提前限速限量；使用媒体探测验证内容；外部上传采用流式读取；增加磁盘水位、总配额、孤儿文件巡检及并发压测。

### R19：本地 ASR 非阻塞、队列与 TTL — FAIL

**证据**

- `tools/local-asr/server.py` 相对原基线没有整改差异。
- `tools/local-asr/server.py:85-92,148-168` 的 async submit 路径仍直接调用同步 `subprocess.run` 转码，会阻塞事件循环。
- 队列是无界 `queue.Queue()`，`TASKS` 没有 TTL；排队任务也写成 running，重启后状态丢失。

**整改要求**

转码与识别都进入受控 worker；队列设置容量并返回 429/503；区分 queued/running；结果和临时文件按 TTL 清理；增加健康检查延迟、队列溢出和结果过期测试。

### R20：详情随轮询更新 — PASS

**成立证据**

- `src/pages/MeetingBoardPage.tsx:46,58-59` 只保存 `detailId`，从最新任务列表派生详情。
- `src/pages/MeetingBoardPage.tsx:65-71` 通过 `inFlightRef` 防止同一轮询请求重叠。

**改进建议**

上传与重跑各自创建额外 2 秒轮询，可能叠加，组件卸载前也没有统一清理；常驻 10 秒全量轮询应按页面可见性和任务运行状态降频。

### R21：设置草稿保存与测试 — PARTIAL

**已成立**

- `src/components/ModelSettings.tsx:88-100,117-118` 在保存失败时保留草稿。
- `src/components/ModelSettings.tsx:136-150` 会阻止地址、模型或 provider 改变后使用旧打码密钥测试。

**未成立**

- `testDraftForm` 只有 `try/finally`，没有 `catch`（第 132–155 行）；网络异常可能形成未处理 Promise，也没有明确提示。
- 保存后首页 meta 不刷新，通道状态可能继续显示旧值。

### R22：设置字段保留与并发编辑 — PARTIAL

**已成立**

- `server/server.cjs:285-333` 使用 `{...prev, ...}` 保留未知字段；ASR 轻量更新也保留其他设置。

**未成立**

- 没有 version、ETag 或 `If-Match`，两个浏览器仍会互相覆盖整份 models 设置，没有明确冲突提示。
- 额度日期用 `new Date().toISOString()` 的 UTC 日期（`server/pipeline.cjs:69-75`、`server/server.cjs:473-477`），在中国时区跨日附近会与用户自然日不一致。

### R23：弹窗焦点、尺寸与多层可访问性 — PARTIAL

**已成立**

- `src/components/Dialog.tsx:22-51,55-67` 使用 ref 管理 onClose，避免轮询重渲染导致焦点重置，并增加最大高度、内部滚动和背景滚动锁定。

**未成立**

- 打开“整条重跑”时详情 Dialog 仍挂载（`src/pages/MeetingBoardPage.tsx:434-503`），同时存在两个 `role=dialog`/`aria-modal`；底层没有 `inert` 或 `aria-hidden`，辅助技术不能确认哪个是顶层。
- 没有键盘焦点、320px 窄屏和低高度视口的浏览器回归证据。

## 4. 新增功能复审

### 4.1 转写双通道与快捷切换 — FAIL

存在确定的字段契约断裂：

- 设置页和 `server/server.cjs:275-278,321-333` 使用 `apiKey` + `apiSecret`。
- `server/iflytek.cjs:20-21,87-98` 只识别 `secretKey`，不读取 `apiSecret`/`apiKey`。
- `/api/meta` 和重跑预览主要根据 `appId` 判断已配置，可能显示真实讯飞通道；实际适配器却因缺少 `secretKey` 静默进入 mock。
- `meeting.secret.example` 仍使用 `appId/apiKey/secretKey`，进一步证明配置契约不一致。

这是 P1 数据真实性问题。应定义一个共享配置 schema，保存、读取、状态判断、测试和执行全部复用；生产环境禁止静默 mock；增加“从 UI 保存后执行一次真实 provider 调用”的契约测试。

### 4.2 两档重跑与额度提示 — PARTIAL

- UI 和 API 已提供只重跑分析与整条重跑，且显示云/本地额度口径。
- 预览与确认没有版本快照绑定；另一会话在确认前切换通道时，实际执行口径可能与弹窗不同。
- 队列满时先持久化 queued 再入队，失败不回滚，是会使任务永久卡住的高优先级缺陷。
- 额度跨日按 UTC 计算，用户看到的自然日口径可能错误。

### 4.3 说话人手动标注与纯重渲染 — FAIL

- speaker PUT 在任务运行中没有 409 互斥，并直接写 transcript、tasks 和 HTML，可与流水线竞争，破坏 R07/R09 的一致性。
- “重跑分析”会先把映射后的真实姓名写入分析结果。以后把 Alice 改成 Bob，纯重渲染只替换 `说话人N`，旧姓名 Alice 不会被替换；统计图可能显示 Bob，正文仍显示 Alice。
- speaker 名称还可触发 R16 的 tooltip DOM XSS。

应保留不可变的 speaker ID 和未映射 canonical analysis，所有姓名只在最终渲染阶段映射；标注端点使用运行互斥、原子写和版本冲突检查。

### 4.4 进度条 — PARTIAL

- `src/lib/meeting.ts:160-176` 有阶段权重和耗时估算，看板与详情均展示进度。
- 这是客户端固定时长估算，不是服务端真实进度；对长录音和不同通道可能明显误导。
- 元素没有 `role=progressbar` 和 `aria-valuenow`，只有容器 aria-label。

界面应明确写“预计进度”，按音频时长和 provider 校准，并由服务端上报可解释阶段；补充无障碍属性。

## 5. R24 与 R25

### R24：核心回归测试 — FAIL

- 已跟踪的新增测试主要是 `scripts/regression_batch1.py`，但当前路径、认证、进程探测和断言口径均有问题，不能在 HEAD 上复现。
- 仓库外的 `_verify_*.py`、`_unit_b456.cjs` 等未被 Git 跟踪，且部分包含绝对路径，不能作为团队或 CI 的验收证据。
- `scripts/verify.mjs` 中“键盘排序”实际使用 DOM click，分页只验证任意 `aria-current`，详情选择第一个 `aria-haspopup=dialog`，无法证明对应业务行为。
- 未覆盖认证、CSRF、SSRF、配置损坏、部署回滚、四种模拟组合、队列满、原子写故障、双通道、两档重跑、说话人重命名、进度、R20–R23 浏览器矩阵。

**最低测试分层建议**

1. 单元/契约：配置 schema、normalize、采样、ID、speaker 映射、额度日期。
2. API 集成：认证/CSRF、并发重跑、队列满、文件清理、原子写故障、配置损坏。
3. Provider stub：断流、慢响应、429、503、重定向、超大响应、字段契约。
4. 浏览器：焦点、双 Dialog、320px/低高度、轮询更新、网络失败草稿保留。
5. 部署：不可变镜像、候选失败、健康单项失败、真实旧镜像回滚和版本一致性。

### R25：性能基线 — FAIL

没有提交可重复的性能基线。当前热点仍包括：

- `GET /api/tasks` 每次全量读取任务，并对每条同步执行文件存在检查（`server/server.cjs:27-33,424`）。
- 前端全量拉取、全量 `JSON.stringify` 搜索（`src/lib/meeting.ts:89`、`src/components/DataTable.tsx:92-100`）。
- 页面常驻 10 秒全量轮询，并可能叠加 2 秒轮询。
- JSON 状态每次整文件写入，任务增长后写放大明显。

先建立 100、1,000、10,000 任务三档数据集，记录 API p50/p95/p99、响应体大小、event-loop lag、RSS/heap、磁盘写入量、浏览器首次渲染和搜索耗时。本轮基线冻结后，再评估服务端分页、任务摘要、增量事件/SSE、索引和分片存储。

## 6. 新发现问题清单与优先级

| 优先级 | 问题 | 影响 |
|---|---|---|
| P0 | R11 回滚保存可变 `:cand` 标签 | 发布失败时不能可靠恢复旧版本 |
| P0 | R16 ECharts tooltip DOM XSS | 打开恶意说话人名称的纪要并悬停即可执行脚本 |
| P0 | `auth.json` 损坏后恢复公开默认密码 | 配置损坏可变成管理员认证绕过 |
| P1 | 讯飞 UI 字段与适配器字段不一致 | 页面显示已配置，任务可能静默使用 mock，内容真实性失守 |
| P1 | 重跑先写 queued，队列满不回滚 | 任务永久卡住，随后不能重跑/删除 |
| P1 | 说话人名称固化进分析结果 | 再次重命名后正文和统计使用不同姓名 |
| P1 | speaker PUT 无运行互斥且绕过原子写 | 与流水线竞争，任务/转写/纪要可能不一致或损坏 |
| P1 | R19 本地 ASR 整改未进入源码 | 转码阻塞、无界队列、无 TTL 仍存在 |
| P1 | LLM 地址无允许规则且测试无限流 | SSRF、内网探测和密钥外送风险仍在 |
| P1 | 原子写只覆盖部分路径 | API 写入仍可能损坏全部任务或静默清空设置 |
| P2 | 生成日期仍使用上传日期 | 重跑纪要日期错误 |
| P2 | 残缺 LLM JSON 被标完整并显示“无” | 用户把缺失内容误认为没有内容 |
| P2 | 审计日志允许控制字符注入 | 攻击者可伪造日志行，影响追责 |
| P2 | GitHub、本地和生产版本不一致 | 无法证明被评审代码就是已发布代码 |

## 7. 建议整改顺序与重新验收门槛

### 批次 A：立即阻断风险

1. 修复认证配置 fail-open、登录限流和审计注入。
2. 修复 tooltip XSS，并对 speaker 全部输出上下文做恶意输入测试。
3. 修复讯飞配置 schema，生产禁止静默 mock。
4. 修复部署不可变镜像、健康检查和真实回滚。

### 批次 B：任务与数据一致性

1. 修复队列满回滚。
2. 所有状态写入统一走原子持久化模块。
3. speaker 映射改为 ID 驱动，增加运行互斥和版本检查。
4. 完成本地 ASR worker、队列容量和 TTL。

### 批次 C：内容正确性

1. 修正三类日期语义与行动项证据链。
2. 长会中段覆盖改为分块处理。
3. 严格 schema 区分缺失、空值和截断。
4. 完善外部调用截止、响应上限和分类重试。

### 批次 D：可复现验收

1. 修复并提交测试，不依赖未跟踪文件或开发机绝对路径。
2. 在全新 clone 的 CI 中构建并执行测试。
3. 建立性能基线。
4. 将同一个 commit SHA 推送 GitHub、构建不可变镜像并部署；`/api/version` 必须返回该 SHA。
5. 在生产只读复核版本和认证后，使用明显标记的测试文件完成同名上传、两档重跑、双通道、speaker 重命名和清理测试。

在 P0/P1 问题关闭、回归测试可重复通过、GitHub 与生产提交号一致之前，不建议把本轮状态标记为“整改验收完成”。

## 8. 给代码 AI 助手的执行要求

代码 AI 应逐条读取本报告，以“原验收标准 + 本报告新增缺陷”为完成定义。每个批次开始前列出计划、涉及文件、风险和测试；每项修复必须提交能在全新 clone 中运行的回归用例，并记录命令、预期结果和实际结果。不要仅根据构建通过、接口返回 200 或人工页面截图宣告完成。禁止通过放宽断言、删除错误提示、静默回退 mock 或跳过故障场景来让测试通过。涉及现有数据格式迁移时，先给出向后兼容与回滚方案；涉及数据库操作必须先取得用户明确同意。全部完成后，提供唯一 commit SHA、镜像 digest、生产 `/api/version` 输出和逐项 R01–R25 证据矩阵，再提交独立复审。
