# MMBoard 第二轮独立整改复审报告

> 复审日期：2026-09-22  
> 评审角色：独立评审方，只验证、裁定和提出整改要求，不参与代码建设。  
> 上轮报告：`MMBOARD_REASSESSMENT_2026-09-21.md`  
> 开发方交付说明：`MMBOARD_交付复审_2026-09-22.md`  
> 代码复核基线：`990a5b7`；当前 GitHub `main` 另含文档提交 `83574d4`。

## 1. 总体结论

本轮整改解决了上轮多项高风险问题，两套交付脚本在复审环境中均可运行并通过，但开发方证据矩阵把若干未完成或未覆盖的问题提前标为 `CLOSED`。本轮结论如下：

- **PASS：10 项**：R01、R04、R07、R08、R10、R14、R16、R20、R21、R25。
- **PARTIAL：15 项**：R02、R03、R05、R06、R09、R11、R12、R13、R15、R17、R18、R19、R22、R23、R24。
- **FAIL：0 项**。相比上一轮已有实质改善，但尚未达到“R01–R25 全部关闭”。

本轮新增或重新确认的主要问题：

1. R02 删除当天最新任务后，下一个任务会复用相同展示编号。
2. “重新跑分析”仍把人工姓名写入送给 LLM 的文本，第二次改名后旧姓名可能固化在分析正文中。
3. 候选容器和正式容器同时挂载同一个可写生产数据卷，候选启动恢复逻辑可能修改正式任务状态。
4. `server/meeting.secret.json.bak` 没有被 `.gitignore` 忽略，当前工作区存在包含真实配置的未跟踪备份文件。
5. LLM 在非 demo 的生产配置缺失时仍静默使用 mock，与交付文档“mock 仅显式 demo”不符。
6. GitHub、本地 HEAD 与生产提交号仍不是同一 SHA。

## 2. 版本与可复现验证

### 2.1 实际版本

| 对象 | 实际值 | 裁定 |
|---|---|---|
| 代码整改提交 | `990a5b729c823b481f2c91d952841ee8980492a7` | 本轮功能代码基线 |
| 当前本地 HEAD | `83574d4e93187a7b5053879e3b204194087781c7` | 在 `990a5b7` 之上增加交付文档 |
| GitHub `main` | `83574d4e93187a7b5053879e3b204194087781c7` | `git ls-remote` 实测 |
| 生产 `/api/version` | `990a5b7` | 生产仍运行功能提交 |

代码内容可以按 `990a5b7` 复核，但交付文档声明的“本地、GitHub、生产三方同一提交”不准确。若允许文档提交不触发部署，应将口径写成“生产代码树与 `990a5b7` 一致，GitHub HEAD 为仅文档变更的 `83574d4`”，不能写成三方 SHA 一致。

### 2.2 本轮实际执行结果

| 命令 | 结果 |
|---|---|
| `node scripts/verify_batch_a.cjs` | 70/70 通过 |
| `python scripts/regression_batch1.py` | 27/27 通过 |
| `npm run typecheck` | 通过 |
| `npm run build` | 通过；仍提示 `./fonts/noto.css` 构建期不存在 |
| R02 删除最新编号隔离复现 | 失败：删除 `...-002` 后下一条再次生成 `...-002` |
| GitHub `main` 只读核对 | `83574d4` |
| 生产 `/api/version` 只读核对 | `990a5b7` |

两套脚本通过是真实证据，但部分断言只验证源码中存在某个字符串，不能等同于运行期行为。例如部署、R19、本地 ASR 和队列顺序有较多静态字符串断言。

## 3. 三项有意取舍的裁定

### 3.1 R02 API 主键仍使用展示编号：不接受 CLOSED，维持 PARTIAL

使用 UUID 作为产物目录键是正确改进，也解决了不同任务共享输出目录的问题。保留展示编号作为 API 路径本身可以接受，前提是展示编号不可复用或 API 同时使用不可复用身份校验。

当前 `server/pipeline.cjs:163-205` 只从现存任务和形如 `MT-*` 的旧输出目录计算最大序号。新任务输出目录已经改成 UUID，因此删除当天序号最大的任务后，该序号不再留下任何持久化占用记录。隔离复现结果：

```json
{"first":"MT-20260922-001","deletedLatest":"MT-20260922-002","next":"MT-20260922-002","reused":true,"dirKeysDistinct":true}
```

这意味着旧书签、审计记录或外部调用保存的 `/api/tasks/MT-...-002` 以后可能指向另一条任务。现有回归只测试“删除中间 002，但 003 仍存在”，没有覆盖原验收明确要求的“删除最后记录”。

**接受条件：** 将每日序列高水位独立持久化，或让 API 使用 UUID；新增“删除当天最大编号、服务重启、再次创建”用例，并断言展示 ID 和 API 身份均不复用。

### 3.2 R13 均匀采样：接受为有效缓解，不接受关闭

均匀采样比原先完全删除中段更好，可以作为当前资源约束下的实现选择，不强制要求 Map-Reduce。但原验收目标是“唯一关键决议位于长会议中段时仍能提取”，均匀抽行只能提高命中概率，不能保证任意中段决议被保留。

`server/llm.cjs:241-250` 只选择固定步长行。测试把标记放在恰好被采到的 `MIDDLE_MARKER_500`，没有把标记放在相邻的未采样行，也没有验证 LLM 最终提取结果。

**裁定：R13 PARTIAL。** 可以保留均匀采样，但产品必须持续显示覆盖率和“未覆盖内容可能遗漏”的警告；若要关闭原验收，应采用分块提取或能保证关键事件扫描的方案，并测试多个非采样点。

### 3.3 R17 不做讯飞轮询瞬态重试：维持 PARTIAL

LLM 总截止、响应体上限和确定性 4xx 不重试均已改善。但开发方明确承认讯飞轮询瞬态错误仍可能直接失败，也没有 429、503、断流的 provider stub 故障测试。

**裁定：R17 PARTIAL。** 该取舍可以作为暂时已知限制，不满足原故障验收标准。

## 4. R01–R25 复审矩阵

| 项 | 本轮结论 | 核心依据与剩余问题 |
|---|---|---|
| R01 | PASS | 同名存储键、原始名、删除隔离和重跑均有端到端断言。 |
| R02 | PARTIAL | UUID 产物目录有效；删除当天最大编号后仍会复用展示/API ID。 |
| R03 | PARTIAL | fail-closed、随机初始密码、限流、审计清洗和撤销一致已验证；仍是全权单管理员模型，且首次初始化 `auth.json` 在 `server/auth.cjs:47` 直接写入，没有交付文档宣称的原子写和显式 0600。 |
| R04 | PASS | 匿名任务/纪要拒绝，旧静态输出路径关闭。 |
| R05 | PARTIAL | LLM 地址校验和测试限流已增加；`allowPrivateLlmHosts` 实际是全局布尔开关，不是主机白名单；DNS 校验与真实连接分两次解析，存在 DNS rebinding 时间窗；本地 ASR 重定向仍未逐跳复验。 |
| R06 | PARTIAL | 讯飞生产缺配置会失败，模拟横幅保留；但 `server/llm.cjs:93-96` 在非 demo 环境缺 LLM 配置时仍静默 mock，交付文档“mock 仅显式 demo”不成立。 |
| R07 | PASS | 重跑、删除互斥和 runId 归属检查成立。 |
| R08 | PASS | 入队前检查避免了上轮永久 queued 缺陷；队列恰好满的 HTTP 时序仍建议补运行测试。 |
| R09 | PARTIAL | 任务、设置、额度等主要状态已统一持久化；首次 auth 初始化、analysis JSON 和部分产物仍直接写。固定 `.tmp` 名也不支持多进程写入。 |
| R10 | PASS | 清单含 `persist.cjs`，版本接口正常。 |
| R11 | PARTIAL | 不可变 image ID 和逐项健康检查已修复；但候选与正式容器同时挂载同一可写生产数据卷。候选启动会执行任务恢复，可能把正式容器正在处理的任务标为中断，并造成两个进程竞争 JSON 文件。失败回滚也尚未真实演练。 |
| R12 | PARTIAL | 主 secret 未入库；但 `.gitignore` 只忽略 `server/meeting.secret.json`，没有忽略 `.bak`。当前 `server/meeting.secret.json.bak` 未跟踪且含真实配置。首次 auth 文件也没有显式 0600。自动 secret 扫描仍缺失。 |
| R13 | PARTIAL | 接受均匀采样为改善；不能保证任意中段唯一决议被保留。 |
| R14 | PASS | 缺失字段会标为 partial，并明确显示缺少章节。 |
| R15 | PARTIAL | 上传时间、生成时间和未知会议日期已分开；行动项 `tref/quote` 仍可选且未验证与 transcript segment 的真实性，原“可追溯”只部分成立。 |
| R16 | PASS | tooltip 使用编码后的 `speakerHtml`；恶意 speaker 测试通过。 |
| R17 | PARTIAL | LLM 边界改善；讯飞瞬态轮询与 provider 故障矩阵未完成。 |
| R18 | PARTIAL | 前置扩展名过滤、大小、限流、失败清理成立；内容探测、流式外部上传、磁盘水位和总配额未完成。 |
| R19 | PARTIAL | worker、有界队列、queued/running 和 TTL 已进入代码；验证仅为源码字符串断言，未在 FunASR 环境实测健康延迟、队列溢出与 TTL。 |
| R20 | PASS | 详情从最新任务状态派生且防请求重叠。 |
| R21 | PASS | 网络异常提示、草稿保留和保存后 meta 刷新已实现。 |
| R22 | PARTIAL | 主设置 PUT 的 version/409 成立；`/api/settings/asr` 不校验也不递增 version，旧客户端缺 version 时仍放行，仍有绕过并发保护的写路径。 |
| R23 | PARTIAL | inert、aria-hidden 和顶层焦点处理已改善；320px、低高度、键盘和读屏浏览器证据仍缺。 |
| R24 | PARTIAL | 两套脚本可复现且本轮通过；浏览器矩阵、provider 故障注入、部署失败演练和真实 FunASR 测试仍缺，多项断言仅为源码字符串检查。 |
| R25 | PASS | 100/1,000/10,000 三档基线已入库，可作为后续优化比较起点。 |

## 5. 新发现和未关闭的高优先级问题

### 5.1 P1：第二次说话人改名仍可能出现正文与统计不同名

`server/server.cjs:572-574` 在“重新跑分析”时先对 transcript 文本执行 `applyMapToText`，把 `说话人N` 替换成当前真实姓名，再交给 LLM。虽然提示词要求模型输出 `说话人N`，输入中对应标签已经被替换，提示词无法可靠恢复编号。

随后 `server/pipeline.cjs:351-357` 把带旧姓名的分析结果保存到 `analysis.json`。以后从“张三”改为“李四”，`applySpeakerMapDeep` 只替换 `说话人N`，不能替换已经固化的“张三”。现有生产测试只验证第一次改名，没有验证“标注 A → 重跑分析 → 改成 B → 纯重渲染”。

**修复要求：** LLM 永远接收带稳定 speaker ID 的 canonical transcript；姓名只在最终渲染层映射。增加二次改名回归，并断言正文、行动项、统计图和 tooltip 全部只出现新姓名。

### 5.2 P1：候选容器可修改正式生产数据

`deploy/deploy.cjs:138-140` 让候选容器直接挂载生产数据卷，同时旧正式容器仍在运行。服务启动会执行中断恢复和读写状态文件，这会让候选对正式任务产生副作用，也会让两个 Node 进程竞争同一组 JSON 文件。

**修复要求：** 使用生产卷只读快照或复制出的候选数据目录；健康检查不得修改生产状态。若必须验证迁移，使用专用迁移检查命令并在切换点独占数据卷。

### 5.3 P1：真实密钥备份文件未忽略

交付文档声称已补 `.bak` 忽略规则，但实际 `.gitignore` 没有该规则，`git check-ignore server/meeting.secret.json.bak` 无匹配，`git status` 显示该文件为未跟踪。复审只确认文件包含真实配置，没有在报告中记录任何密钥值。

**修复要求：** 立即把 `server/meeting.secret.json*` 或明确的备份模式加入忽略规则，删除工作区明文备份并轮换其中凭据；在 CI 增加 secret 扫描。删除和轮换属于用户数据/外部配置操作，应由用户明确授权后执行。

### 5.4 P1：生产缺少 LLM 配置仍静默 mock

`server/llm.cjs:93-96` 在没有可用 LLM 配置时直接返回 mock；只有讯飞通道实现了“非 demo 缺配置明确失败”。这与交付文档 R06 的结论不一致，也可能让真实转写生成模拟分析。

**修复要求：** 非显式 demo 环境中，转写和分析任一 provider 缺配置都应明确失败；meta、预览、任务状态和纪要横幅使用同一配置判定函数。

## 6. 对开发方交付结论的最终裁定

- R02 的实现取舍：**暂不接受 CLOSED**。UUID 目录键有效，但展示/API ID 删除最新项后会复用。
- R13 的实现取舍：**接受为有效改善，状态 PARTIAL**。不强制指定 Map-Reduce，但均匀采样无法满足原保证。
- R17 的实现取舍：**接受为已知限制，状态 PARTIAL**。需要在后续补齐讯飞瞬态容错和故障注入。
- 两套自动化脚本：**确认可以运行并全部通过**，但覆盖范围不足以把 R19、R23、R24、部署回滚等项目判为关闭。
- 本轮交付：**可以进入下一轮针对性整改，不应标记为全部验收完成。**

## 7. 下一轮最小整改顺序

1. 先处理密钥 `.bak` 暴露并轮换凭据。
2. 修复候选容器写生产数据卷的问题，并做一次可验证的失败回滚演练。
3. 修复 speaker 二次改名的 canonical 数据模型并加端到端回归。
4. 持久化 R02 序列高水位或把 UUID 用作 API 身份，覆盖删除最后记录和重启。
5. 禁止非 demo 的 LLM 静默 mock。
6. 将 settings/asr 纳入 version 冲突控制。
7. 补 FunASR 运行测试、浏览器矩阵和 provider 故障注入。

每项完成后应提供实际运行断言，避免用 `source.includes(...)` 代替行为验证。最终交付文档应区分“代码提交”“仅文档提交”和“生产镜像对应提交”，准确说明它们的关系。

