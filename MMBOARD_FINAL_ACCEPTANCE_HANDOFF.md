# MMBoard 最终验收目标与工作移交指导

> 用途：统一项目方、开发 AI 和独立评审方对“做到什么程度才算验收完成”的理解，避免把目标误解为机械地把 R01–R25 全部标成 PASS，也避免整改范围无限扩大。

## 1. 最终验收目标

MMBoard 的最终验收目标是：

> **系统能够在当前内网、单管理员、单实例的实际使用条件下，稳定处理真实会议文件；不会泄露会议内容或敏感配置，不会把模拟、缺失或错误内容伪装成真实结果；任务失败后可以恢复和排查；新版本可以安全发布和回滚；关键行为有可重复的自动化证据。**

R01–R25 是帮助达到上述目标的问题清单，不是项目目的本身。某项是否必须关闭，应依据它是否影响安全、数据正确性、核心业务、恢复能力和可验证性，而不是只看编号或文字表述。

## 2. 六个验收维度

### 2.1 结果可信

- 真实转写、模拟转写、真实分析和模拟分析必须明确区分。
- 生产环境只有显式开启 demo 才能使用 mock，缺少真实配置必须明确失败。
- 长会议不能静默丢失中段关键决议；分块处理必须把每块真实原文发送给模型。
- 行动项必须能定位到对应原文和时间范围；无法核验时明确显示警示。
- 说话人多次改名、重跑分析和纯重渲染后，正文、行动项、统计和图表必须一致。
- LLM 输出缺字段、类型错误或被截断时，不能把缺失内容显示成已经确认的“无”。

### 2.2 任务和数据可靠

- 同名文件、并发创建、删除、服务重启和重跑不能串任务或共享产物。
- 展示编号和内部主键不能错误复用；删除当天最新任务并重启后仍不能指向新任务。
- 队列满、磁盘不足、额度文件损坏和状态文件损坏时必须明确失败，不能永久卡在 queued、静默回零或漏记额度。
- 任务、设置、认证、额度、转写和分析状态采用统一原子持久化和可验证恢复策略。
- 服务重启后，运行中的任务必须恢复或明确标为中断。

### 2.3 访问和安全边界可靠

- 未登录者不能读取或修改任务、转写、纪要和设置。
- 所有可配置的外部地址，包括真实调用和“测试连接”接口，都必须使用同一套目标校验。
- LLM、本地 ASR 的 DNS 解析、IP 绑定和重定向逐跳校验不能存在旁路。
- 本地服务的放行范围应尽量使用主机和端口列表；如保留私网 CIDR，必须明确风险和管理边界。
- 说话人名称、文件名和会议内容在正文、HTML、属性、脚本和图表 tooltip 中都只能作为普通文字。
- 密钥不得进入 Git、镜像、部署归档和测试日志。

### 2.4 外部服务故障可控

- 讯飞、LLM 和 FunASR 出现超时、断流、429、5xx、业务错误或空结果时，应有限重试或明确失败。
- 只重试可恢复错误；认证、权限和参数错误立即失败。
- 重试必须有次数上限、退避和请求记录，不能无限等待或产生不可控费用。
- 本地转写的转码和识别不能阻塞 MMBoard 主服务。
- 队列满时明确拒绝，结果和临时文件按 TTL 清理。

### 2.5 部署可以安全恢复

- 功能代码提交、GitHub 文档提交、镜像 digest 和生产 `/api/version` 的关系必须准确说明。
- 候选容器不能修改生产数据，应使用隔离快照。
- 健康检查逐项验证状态码、响应体和预期 commit。
- 发布失败后必须恢复到发布前的不可变镜像，并验证回滚后的准确 commit。
- 部署归档和镜像不得包含敏感配置。

### 2.6 验收证据可以复现

- 新人在全新 clone 中按文档命令即可运行测试。
- 测试所需音频、配置模板和 stub 必须随仓库提供，或由脚本自动生成。
- 测试必须验证运行行为，不能仅搜索源码字符串。
- stub 必须根据收到的真实输入生成响应，不能按块编号直接返回预期答案。
- API、浏览器、故障注入、部署、性能和真实 FunASR 分别具有适当覆盖。
- 测试报告必须同时保留成功、失败和环境限制，不能只记录通过项。

## 3. 当前必须完成的收尾项

依据第四轮独立复审，以下项目应在本次验收结束前处理：

### 3.1 R13：修复超长会议分块提取

当前 `mapChunk` 没有把真实 chunk 加入提示词，必须修复。测试应证明：

- 每个 map 请求包含对应分块原文。
- 第一块不包含第二块的唯一标记。
- 第二块包含唯一标记，stub 只有看到该标记才允许返回。
- reduce 输入和最终 decisions 均包含该决议。
- map 空响应、错误 JSON、单块失败和三块以上会议具有明确行为。

这是当前唯一 FAIL，也是本次验收的阻断项。

### 3.2 R05：消除设置测试接口的 SSRF 旁路

- `/api/settings/test` 必须复用真实 LLM 调用的 DNS 解析、精确白名单、IP 绑定和禁止重定向机制。
- `/api/settings/test-asr` 必须复用 `guardedFetch`，不能直接调用默认 `fetch`。
- 精确白名单配置必须同时作用于保存、测试、预览和执行。
- 迁移旧 `allowPrivateLlmHosts` 后应删除全私网放行兼容开关。

### 3.3 R09：额度损坏必须在付费调用前失败

- 主备额度文件同时损坏时，不得调用讯飞后再忽略记账错误。
- 在发起付费转写前完成额度状态读取和可写性检查。
- 错误应明确显示需要人工恢复，不得静默回零或继续消费额度。

### 3.4 R15：把 quote 与 tref 绑定到同一原文片段

- 先根据 tref 选出重叠 segments。
- 只在这些 segments 中核验 quote。
- 校验开始时间小于结束时间，且不超出音频范围。
- 引用来自其他时间段时必须标记为未核验。

### 3.5 R18：补齐资源治理边界

- 总配额计算必须包含本次上传的 Content-Length。
- 并发上传需要容量预留，不能都读取同一个旧缓存后共同突破配额。
- ffprobe 改成异步、有限并发的 worker，避免阻塞 Node 事件循环。
- 代理层设置请求体大小、超时和速率限制。

### 3.6 R19/R24：让真实 FunASR 测试可以直接复现

- 提交授权清晰、体积合理的语音夹具，或由脚本自动生成含已知文字的 TTS 音频。
- 默认命令不得依赖未提交的 `_funasr_t1.wav`。
- 测试至少覆盖健康响应、串行转写、并发三任务、文本非空、结果不串扰和失败清理。
- 真实 FunASR 不可达时，应明确报告环境阻断，不能宣称测试通过。

## 4. 可以转入后续迭代的事项

以下事项在核心收尾完成后，可以作为正常产品迭代，不需要无限延长本次验收：

- 更大规模的服务端分页、增量更新和事件推送。
- 更复杂的长会议语义合并和跨块去重。
- ASR 模糊文本相似度和语音级证据对齐。
- NVDA、VoiceOver 等真实读屏器的深度听读抽样。
- 多实例部署、分布式锁和数据库化存储。
- 更大规模的性能与容量优化。
- 镜像层通用 secret scanner 的 CI 集成。

这些事项应分别建立需求、风险和验收标准，不再混入本轮 R01–R25 的收尾循环。

## 5. 停止整改、进入正常维护的标准

满足以下条件后，独立评审应结束本轮整改，不再为了追求形式上的“全部完美”持续扩大范围：

1. R13 的实现错误和测试假阳性已经修复。
2. R05 的两个设置测试接口没有安全旁路。
3. R09 的额度损坏不会造成未记账的真实调用。
4. R15、R18、R19 达到双方接受的实际使用标准。
5. 所有验收脚本能在全新 clone 中按文档直接运行；环境依赖说明真实准确。
6. 完成一条真实业务链路：上传 → 转写 → 分析 → 说话人标注 → 重跑 → 下载纪要。
7. 完成一条故障链路：外部服务失败、队列满、状态损坏、发布失败回滚。
8. GitHub、功能提交、镜像 digest 与生产版本关系准确可追溯。
9. 独立复审未发现新的 P0/P1 缺陷。
10. 凭据轮换由用户单独决定并记录，不与代码验收混为一谈。

## 6. 开发 AI 的工作方式

- 每批开始前说明计划、涉及文件、风险和验收命令。
- 修改后列出所有变更文件及其原因。
- 先写能够暴露现有缺陷的失败用例，再修改实现。
- 不得通过放宽断言、硬编码期望结果、按测试编号返回答案或静默降级让测试通过。
- 构建通过、类型检查通过、HTTP 200 和截图都不能单独证明业务验收成立。
- 测试必须检查输入是否真正进入被测路径，以及输出是否由该输入产生。
- 不修改生产数据；涉及数据库操作必须先询问用户。
- 凭据删除、轮换及外部平台操作必须由用户明确授权。
- 最终提交必须提供功能 commit、GitHub HEAD、镜像 digest、生产 `/api/version` 和逐项证据矩阵。

## 7. 可直接复制给开发 AI 的移交话术

请完整阅读 `MMBOARD_FINAL_ACCEPTANCE_HANDOFF.md` 和 `MMBOARD_FOURTH_REASSESSMENT_2026-09-22.md`，以“结果可信、任务可靠、安全边界可靠、外部故障可控、部署可恢复、证据可复现”为最终验收目标，不要把工作简化为机械地把 R01–R25 改成 PASS。本轮先关闭 R13 阻断项：将真实 chunk 放入 map prompt，并重写 stub，使其只有在收到实际中段决议时才返回该决议；随后修复两个设置测试接口绕过安全请求器、额度状态损坏后仍继续付费转写、quote 与 tref 未绑定同一 segment、上传配额并发超额与同步 ffprobe 阻塞，以及真实 FunASR 脚本缺少可复现音频夹具的问题。每项先提交能暴露旧缺陷的失败测试，再修改实现；测试必须验证真实输入经过真实执行路径产生结果，禁止源码字符串匹配、按块编号返回期望答案或放宽断言。每批开始前说明计划、文件、风险和验收方法，完成后列出修改文件与实际输出。达到文档第 5 节的停止标准后，本轮整改结束，其余分页、深度读屏、多实例和高级性能优化转入正常产品迭代。不要修改生产数据，涉及数据库、凭据删除或外部凭据轮换必须先询问用户。

