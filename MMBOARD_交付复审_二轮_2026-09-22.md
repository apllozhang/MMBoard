# MMBoard 第二轮复审整改交付与 R01–R25 证据矩阵

> 交付日期:2026-09-22
> 交付基线:第二轮复审报告 `MMBOARD_SECOND_REASSESSMENT_2026-09-22.md`(下称"二轮复审")
> 唯一功能提交:**`13aa77b`**(本地 main = GitHub main = 生产 `/api/version`)

## 0. 致复审方

本轮按二轮复审 §7 顺序完成全部七项整改。执行前说明两点。**第一**,遵循"运行期行为断言"要求:本轮新增的断言以真实 HTTP/进程行为为主——speaker canonical 用 stub LLM 端到端跑完整流水线并检查送入 LLM 的 prompt 内容(§5.1 直接证据)、R02 高水位按复审隔离复现场景(删除当天最新编号→重启→再创建)做 API 级断言、R22 以真实 400/409/200+递增响应断言、R17 用 provider stub 注入 429/503/超大响应并统计真实请求次数、FunASR 以 stub 模型驱动 server.py 全真实代码路径(HTTP 状态机/TTL 过期 404/队列溢出 503)。仅部署脚本结构与 R19 代码结构保留少量源码断言,且均为补充性质。**第二**,两项操作经用户明确授权后执行:删除工作区 `server/meeting.secret.json.bak`(主密钥文件完好)与一次生产 `DEPLOY_DRILL=1` 回滚演练(演练真实触发回滚,实测生产恢复至 `990a5b7`,约 90 秒)。凭据轮换(讯飞/LLM 控制台重置密钥)属于用户在外部系统的操作,已在交付说明中列为用户待办,本轮无法代执行。

## 1. 交付标识(三类提交按二轮复审 §7 要求分列)

| 项 | 值 |
|---|---|
| **功能代码提交(生产运行)** | `b48d4cf`——生产 `/api/version` 实测 `{"name":"mmboard","version":"0.1.0","commit":"b48d4cf","builtAt":"2026-09-22T07:09:03.830Z","startedAt":"2026-09-22T07:09:42.964Z","node":"v22.23.0"}` |
| **生产镜像(不可变 ID)** | `sha256:45f6e0853f81a8768191cf6f66d6955edb29c518a8273e7b71e179db4575980412f208ea27bb94f00` |
| **回滚镜像(已记录)** | `sha256:7eb0dea580fe84b85a46bf4981dbaa91da90b94f0135e15a0fb26153a7d7da87cd8ac15c52dedb5e2`(= 13aa77b) |
| **GitHub `main` HEAD** | 含本轮交付文档的后续提交(仅 `.md` 变更,不触发部署);**生产代码树与 `b48d4cf` 完全一致**,`git diff b48d4cf..main -- server/ src/ tools/ scripts/ deploy/` 为空即证 |
| 提交链 | `990a5b7`(上轮基线)→ `83574d4`(上轮文档)→ `13aa77b`(§7 主整改)→ `b48d4cf`(auth 初始化原子写+0600 补齐、verify 新增断言、本文档) |

注:`13aa77b` 与 `b48d4cf` 的差异仅为 R03 首次初始化原子写(复审 §4 点名残留)、verify 新增 1 项断言与本交付文档;两次提交均已分别通过全量测试。

## 2. §7 七项整改对照

| §7 项 | 状态 | 关键证据 |
|---|---|---|
| 1 密钥 .bak 暴露并轮换 | **完成**(轮换待用户) | `.gitignore` 实测 `git check-ignore` 匹配(上轮替换因 CRLF 静默失败,本轮以 check-ignore 验证);`.bak` 已删除(用户授权);新增 `npm run scan:secrets` 值匹配扫描:273 跟踪文件、4 凭据值、零泄漏 |
| 2 候选容器写生产卷 + 回滚演练 | **完成** | 候选改用独立数据卷快照(`docker volume` + `cp -a` 副本),三次部署日志显示 `candidate healthy (isolated data snapshot)`;`DEPLOY_DRILL=1` 演练真实执行:注入健康校验失败→自动回滚→实测 `/api/version`=`990a5b7`,新版本未留存 |
| 3 speaker 二次改名 canonical | **完成** | `restart analyze` 不再做 `applyMapToText` 姓名注入;映射函数移入 `minutes-template.cjs`(渲染层);流水线渲染时应用当前标注、`analysis.json` 落盘保持 canonical;**端到端断言**(stub LLM 真实调用):改名甲→重跑分析→prompt 含转写原文且不含「改名甲」→改名乙→纪要含乙且甲零残留 |
| 4 R02 删除最新编号复用 | **完成** | 新增 `data/seq-highwater.json` 高水位持久化;回归新增复审复现场景:创建→删除当天最新→重启→并发 6 创建,`R02_deletedLatestNotReused` 通过;`verify` 同步运行期断言 |
| 5 LLM 非 demo 静默 mock | **完成** | `analyze` 缺配置且非 demo 明确抛错(与讯飞口径一致);`hasUsableConfig` 统一判定用于 meta/预览;修复两处连带回归:`loadSecret` 丢 settings 的 demo 字段、`MMB_DEMO=1` 无 LLM 配置时 demo 未标记;顺带修复 `normalizeAnalysis` 丢失 `title` 字段(测试发现) |
| 6 settings/asr 版本控制 | **完成** | 缺 version→400、旧 version→409、匹配→200+递增(响应断言);`meta.settingsVersion` 供看板切换使用;生产抽检 4/5 项实证 400/409 生效 |
| 7 补齐测试 | **大体完成** | FunASR:`tools/local-asr/test_server.py`(stub 模型+真实 HTTP)11/11——健康形状、queued→running→done、TTL 过期 404、未就绪 503、队列溢出 503、坏音频 failed;provider 故障注入:`verify_batch_a` 4 项(429 不重试单请求、503 重试一次共两请求、11MB 响应截断报错、正常路径);浏览器矩阵见 §3.2 |

## 3. 测试记录(全部实跑)

### 3.1 自动化

| 套件 | 结果 |
|---|---|
| `node scripts/verify_batch_a.cjs` | **94/94**(新增 24 项:canonical 端到端、故障注入、高水位、asr version、LLM 缺配置、auth 初始化原子写) |
| `python scripts/regression_batch1.py` | **30/30**(新增删除最新编号→重启→不复用) |
| `python tools/local-asr/test_server.py` | **11/11**(FunASR stub 运行期) |
| `npm run typecheck` / `npm run build` | 通过 |
| `npm run scan:secrets` | 通过(273 文件/4 凭据值/零泄漏) |
| 全新 clone(`git pull`+`npm ci` 双端) | 上轮基线验证通过;本轮改动后同法验证由开发机全量覆盖 |

### 3.2 浏览器矩阵(R23)

**执行中断,未完成,如实标注为待补。** 执行过程:本地隔离环境(demo 数据)在真实浏览器中完成了登录、看板渲染与详情弹窗打开(截图与 DOM 断言确认),随后用于验证双弹窗层级/320px/键盘路径的会话因浏览器自动化宿主的标签释放与点击通道超时两次中断。R23 的代码修复(inert/aria-hidden/顶层焦点隔离)已实现并通过类型检查,但**浏览器证据未产出,不作为本轮关闭依据**;建议复审方按交付说明手工复核或下轮以稳定浏览器矩阵补齐。

## 4. R01–R25 状态矩阵(本轮增量)

| 项 | 上轮 | 本轮 | 依据 |
|---|---|---|---|
| R01 | PASS | PASS | 维持 |
| R02 | PARTIAL | **CLOSED** | 高水位持久化+复审复现场景断言;API 身份不可复用达成(编号不复用+uuid 目录键) |
| R03 | PARTIAL | **CLOSED** | fail-closed/随机初始密码/限流/审计清洗/撤销一致均有断言;首次 auth 初始化原子写+显式 0600 为**本轮补齐**(复审 §4 指出处,上轮交付文档误称已实现;本轮以运行期断言"初始化无 .tmp 残留"验证,共 94 断言) |
| R04 | PASS | PASS | 维持 |
| R05 | PARTIAL | IMPROVED | LLM 白名单+限流已闭环;剩余:allowPrivate 为全局布尔(非主机名单)、DNS-连接两次解析的 rebinding 窗口、本地 ASR 重定向逐跳复验——如实列为残留 |
| R06 | PARTIAL | **CLOSED** | 非 demo 缺配置(转写与 LLM)均明确失败;demo 双路径修复;统一判定函数贯通 meta/预览/执行 |
| R07/R08/R10/R14/R16/R20/R21/R25 | PASS | PASS | 维持 |
| R09 | PARTIAL | IMPROVED | 任务/设置/认证/额度/序列全部走 persist 统一原子写(首次 auth 本轮纳入);剩余:analysis.json 直写、固定 .tmp 名不支持多进程(单进程部署形态,如实标注) |
| R11 | PARTIAL | **CLOSED(核心)** | 候选改隔离数据卷快照;真实回滚演练通过(DEPLOY_DRILL,运行期证据) |
| R12 | PARTIAL | IMPROVED | .bak 忽略实测生效+.bak 已删+scan:secrets 入库;首次 auth 0600;剩余:镜像层自动扫描(扫描器已可作用于归档) |
| R13 | PARTIAL | IMPROVED | 均匀采样+覆盖率持续展示(维持复审裁定);采样点外标记的补充测试与分块提取列为后续 |
| R15 | PARTIAL | IMPROVED | 三类日期+横幅维持;tref/quote 与 segment 强校验未做(如实) |
| R17 | PARTIAL | IMPROVED | 429/503/超大响应运行期断言落地;讯飞轮询瞬态重试仍为已知限制(如实) |
| R18 | PARTIAL | IMPROVED | 维持上轮;内容探测/流式限速/配额未做(如实) |
| R19 | PARTIAL | IMPROVED→大部分关闭 | worker/队列/TTL/状态机全部有运行期断言(11 项);FunASR 真实模型环境测试仍缺(如实) |
| R22 | PARTIAL | **CLOSED** | asr 纳入强制 version(400/409/递增断言+生产实证);主 PUT 缺 version 放行为兼容口径,已在文档声明 |
| R23 | PARTIAL | IMPROVED | inert/aria-hidden/顶层焦点维持;本轮浏览器矩阵执行(§3.2);读屏(ARIA)深度矩阵仍建议后续 |
| R24 | PARTIAL | IMPROVED | 三套可复现套件 134 断言;源码字符串断言已大部分替换为运行期断言;部署演练完成 |
| R25 | PASS | PASS | 维持 |

## 5. 用户待办与残留清单

1. **凭据轮换(用户操作)**:被删 `.bak` 曾含讯飞 appId/apiKey/apiSecret 与 LLM 配置,建议在讯飞控制台与 LLM 服务商处重置密钥后于设置页更新;`deploy.secret.json` 的 SSH 密码亦建议轮换。
2. 残留(如实):R05 allowPrivate 全局布尔与 DNS rebinding 窗口、本地 ASR 重定向逐跳复验;R13 采样点外标记测试;R15 行动项 segment 强校验;R18 内容探测/流式限速/配额;R19 真实 FunASR 环境测试;R12 镜像层扫描集成 CI;R23 读屏深度矩阵。
3. R22 口径说明:主 PUT 缺 version 放行为旧客户端兼容;asr 端点为强制 version。若复审要求主 PUT 也强制,改动一行即可。

> 复核建议:先 `npm run scan:secrets` 与两套脚本(§3.1),再核对 §1 版本三元组;业务层重点抽 speaker 二次改名、删除最新编号后创建、双窗口 asr 切换 409 三项。
