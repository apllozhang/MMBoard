# MMBoard 最终验收收尾交付(响应第四轮复审与验收移交文档)

> 交付日期:2026-09-22(UTC);F01/F02 修复增补见 §7(2026-09-23)
> 交付依据:`MMBOARD_FOURTH_REASSESSMENT_2026-09-22.md`(第四轮复审)、`MMBOARD_FINAL_ACCEPTANCE_HANDOFF.md`(验收目标与移交指导)、`MMBOARD_FINAL_ACCEPTANCE_REVIEW_2026-09-23.md`(最终复审,F01/F02)
> 功能提交链:**`6c9a255`**(第四轮六项收尾)→ **`7e55f94`**(最终复审 F01/F02 修复,当前生产运行版本)

## 0. 致复审方:本轮执行方式

按移交文档 §6 的工作方式执行:**先写能暴露旧缺陷的失败用例,再修改实现**。本轮在 `scripts/verify_batch_a.cjs` 中新增/重写了针对六项收尾的全部用例,在旧代码(8e03f66)上实跑为 **红:122 通过 / 25 失败**,修复后同一套件实跑为 **绿:167 通过 / 0 失败**。全部断言验证真实输入经真实执行路径产生结果:

- LLM stub 的 map 分支改为**只回传 prompt 中真实出现的决议标记**(`verify_batch_a.cjs` chunkAware 分支),从机制上排除"按块编号伪造答案";
- 设置测试接口的断言直接走 `POST /api/settings/test`、`POST /api/settings/test-asr`(带 DNS patch 与重定向计数),不再是直接调用底层函数;
- R09 断言用**讯飞 stub 全链路跑真流水线**:额度双坏后上传新任务,以"stub 请求数零增长 + 任务失败且提示人工恢复"证明付费调用未发生;
- 所有失败/边界注入(空响应、坏 JSON、坏音频、预留占满)都在用例中如实构造。

## 1. 交付标识(版本口径)

| 项 | 值 |
|---|---|
| 功能代码提交(生产运行) | `6c9a25501d19783525152f077a4267fcc93c56e8` |
| 生产 `/api/version` | `{"name":"mmboard","version":"0.1.0","commit":"6c9a255","builtAt":"2026-09-22T16:12:19.853Z","startedAt":"2026-09-22T16:12:55.849Z","node":"v22.23.0"}` |
| 生产镜像(不可变 ID) | `sha256:b01a08bf7f51ec0c000d93843ed41fdc732654fd7a8cff28aa6f61249f9c8944`(容器 ale-symphony-console 实测) |
| 回滚镜像(已保留) | `sha256:8bb18f619c116a7a1edc285af9146b2e05f2b77fd5425af4b3244c111c68312d`(= 8e03f66) |
| GitHub `main` HEAD | `6c9a255`(与生产同一提交;`git ls-remote` 实测一致) |
| 提交链 | `8e03f66`(四轮基线)→ `6c9a255`(四轮全部收尾) |

部署过程:候选容器以隔离数据卷快照启动 → 健康检查(版本+认证状态)通过 → 切换 → 生产复验;失败自动回滚路径保持可用(回滚镜像已记录)。

## 2. 逐项整改与验收证据

### 2.1 R13(四轮唯一 FAIL,阻断项):分块原文真实进入 prompt

| 项 | 实现 | 运行期断言(verify,9 项) |
|---|---|---|
| chunk 入 prompt | `mapChunk` prompt 含 `<本块原文>…</本块原文>` 全文与字数 | map#1 含第 1 块原文且**不含**第 2 块标记;map#2 含标记原文(第 900 行) |
| reduce/最终结果 | stub 仅按真实收到的标记回传 | reduce 输入含标记;最终 decisions 含标记;54000 字=2 map+1 reduce |
| 失败行为 | 单块失败不阻塞、如实标注 | 空响应→"第 1 块提取失败"且第 2 块决议仍进结果;坏 JSON→"第 2 块提取失败"且**不伪造**决议进结果 |
| 三块会议 | 2000 行(~82k 字) | 3 map+1 reduce;仅第 2 块 prompt 含标记;最终结果含标记 |

附带发现并修复:旧 `resolveLlmTarget` 丢弃 baseUrl 的 path/query,带路径接口(如 `/api/anthropic`、`/v1`)会被打错地址——现保留 path 与 query(断言:`connectBaseUrl === "http://127.0.0.1:9999/v1"`)。

### 2.2 R05:设置测试接口不再有安全旁路;全放行开关删除

| 项 | 实现 | 运行期断言(verify,11 项) |
|---|---|---|
| test 复用执行路径 | `/api/settings/test` = `resolveLlmTarget`(DNS 解析一次+精确白名单)+ `postJson`(IP 直连、不跟随重定向) | 无白名单→400 提示 allowedLlmHosts;白名单命中→ok 且 stub 收到的 **Host 头为原域名**(证明 IP 直连+Host 保留);302→ok:false 且外部目标计数 0 |
| test-asr 复用 guardedFetch | `/api/settings/test-asr` 弃用裸 fetch | 302→外网被拒(ok:false);内网 /health 正常在线 |
| 布尔开关删除 | `resolveLlmTarget` 删除 `allowPrivate` 参数;`loadSecret` 不再映射;`loadSettings` 自动迁移(boolean true→按激活模型 baseUrl 种入 `allowedLlmHosts`,并从磁盘删除字段) | 传 `allowPrivate:true` 仍拒绝;迁移后 GET 返回种子白名单、磁盘文件无布尔字段、迁移后白名单对测试接口生效 |
| 白名单贯通保存 | GET/PUT 贯通 `allowedLlmHosts`(host 或 host:port,拒绝 URL/空格,上限 64);设置页新增白名单编辑卡片(保存/测试/执行共用同一份) | PUT 保存→GET 回读一致;非法条目→400 |

生产实证:`GET /api/settings` 返回 `allowedLlmHosts:[]` 且无旧布尔;`POST /api/settings/test-asr`(生产,指向真实 FunASR)返回 ok:true。

### 2.3 R09:额度损坏在付费调用前 fail-closed

| 项 | 实现 | 运行期断言(verify,5 项) |
|---|---|---|
| 付费前检查 | `assertQuotaReady()`:记账文件可读(双坏抛错)+ 可写性探测;讯飞非 demo 通道在 `transcribe` **之前**调用 | 额度双坏后上传新任务 → 任务 failed、错误含"额度/人工";**讯飞 stub 请求数零增长**(before=after) |
| 记账失败显式化 | `recordQuotaAfterTranscribe`:转写成功后记账,失败→任务 `quotaRecordFailed=true` + 转写步骤注明"⚠额度记账失败——请人工核对后补记";看板显示"额度未记账"徽标 | 健康路径:任务 done、当日额度>0、无标记;注入记账失败:标记与步骤注记如实落档 |

### 2.4 R15:quote 与 tref 绑定同一片段

实现:先按 tref 选出时间重叠 segments,再**只在这些片段文本中**核验 quote;校验 `from < to`(起止颠倒→invalid)与音频边界(`to` 超过最后一段结束→unverified);仅 quote 无 tref 回退全文。断言 9 项:跨范围引用(quote 取自结尾、tref 指向开头)→ quoteState=unverified;起止颠倒→invalid;超音频→unverified;各警示文案逐项渲染验证;真实证据无警示(按 `<span class="tref-warn">` 元素断言)。

### 2.5 R18:配额预留与异步探测

| 项 | 实现 | 运行期断言(verify,7 项) |
|---|---|---|
| incoming 计入 | `quotaExceeded(incoming)` = 存量 + **在途预留** + 本次 Content-Length | 预留至只剩 1KB → incoming 2KB 即超额;不上传不超额;释放后恢复 |
| 并发预留账本 | 进程内 `reserveUploadQuota`(finish/close 释放,幂等);上传落盘即失效占用缓存 | 预留占满 20GB → 另一个 HTTP 上传被 **503**(真实端点) |
| ffprobe 异步化 | 并发 2 的 worker 池、单次 5s 超时;上传端点 await | 注入慢速 runner:5 并发探测全部完成且**峰值并发 ≤2**;真实 wav→true、不存在→false |

### 2.6 R19/R24:真实 FunASR 压测可复现(仓库零音频)

- 夹具:仓库只提交 `scripts/gen_tts_fixture.ps1`(UTF-8 BOM,兼容 PowerShell 5.1);运行时用 Windows 自带 SAPI 现场合成两段**含已知标记词**的语音(优先中文声音,退回英文),缓存于临时目录;`FUNASR_WAV` 可注入外部音频;SAPI/语音缺失或服务不可达→明确"环境阻断"退出(非零),**不宣称通过**。
- 实测(真实服务 10.10.10.144:8300):**12/12 通过**——健康 7ms;串行 2.0s 转写完成,文本 119 字且含己方标记词、不含他方内容;坏音频→明确 failed;并发 3(A/B/A)6.0s 全部 done,结果**按任务对应各自标记词、零串扰**。

## 3. 测试结果汇总(全部实跑,本轮红→绿)

| 套件 | 红(旧代码) | 绿(修复后) |
|---|---|---|
| `node scripts/verify_batch_a.cjs` | 122/147(25 失败,覆盖全部六项) | **167/167** |
| `python scripts/regression_batch1.py 8793` | — | **30/30**(测试侧日期口径对齐服务端 UTC,产品行为未改) |
| `python tools/local-asr/test_server.py` | — | **11/11** |
| `node scripts/browser_matrix.mjs` | — | **13/13** |
| `node scripts/funasr_load_test.cjs`(真实服务) | 旧版缺夹具不可复现 | **12/12**(SAPI 现场合成夹具) |
| `npm run scan:secrets` | — | 通过(276 文件/4 密钥值/零泄漏) |
| `npm run typecheck` / `npm run build` | — | 通过 |
| 生产抽检 | — | 7/7:版本 6c9a255、admin 登录、allowedLlmHosts 字段贯通、meta 正常、假音频 400(媒体探测)、7 条存量任务完好、test-asr ok:true |

说明:回归脚本首次出现 1 项失败(`R02_idFormatStable`),定位为**测试脚本**用本地日期而任务编号用 UTC 日期(本地已过午夜、UTC 未跨日),对齐为 UTC 后通过——与 R22"额度走本地时区"的约定不冲突(编号口径自始为 UTC,本轮未改产品行为)。

## 4. R01–R25 状态矩阵(收尾后)

| 项 | 上轮(四轮复审) | 本轮 | 依据 |
|---|---|---|---|
| R01–R04、R06–R08、R10、R11、R14、R16、R20–R22、R25 | PASS | PASS | 维持 |
| R05 | PARTIAL | **CLOSED** | 两个设置测试接口复用 resolveLlmTarget/postJson/guardedFetch;全放行布尔已迁移删除;白名单贯通保存/测试/执行;本地 ASR 保留内网 CIDR 作为发现范围(与 LLM 精确列表不同,设置页可编辑精确地址;前轮已提请裁断) |
| R09 | PARTIAL | **CLOSED** | 额度双坏在付费转写**前**拒绝任务(stub 请求数零增长证明);记账失败显式标记+人工核对指引 |
| R12 | PARTIAL | PARTIAL | 凭据轮换按用户决定单独处理(移交文档 §5-10 口径);镜像层扫描 CI 强制转后续迭代 |
| R13 | **FAIL** | **CLOSED** | 分块原文完整入 prompt;stub 仅按真实标记回传;正/负断言+空响应/坏 JSON/三块用例 9 项全绿 |
| R15 | PARTIAL | **CLOSED** | quote 在 tref 对应 segments 内核验;from<to 与音频边界校验;跨范围引用专项警示 |
| R17 | PASS | PASS | 维持 |
| R18 | PARTIAL | **CLOSED(核心)** | 配额含在途预留与本次大小(并发 503 实证);ffprobe 异步 worker(并发≤2 实证);代理层限速为部署侧后续项 |
| R19 | PARTIAL | **CLOSED(核心)** | 真实服务 12/12(健康/串行/并发/内容标记词/不串扰/失败清理);全新 clone 默认命令可运行 |
| R23 | PASS | PASS | 维持(NVDA 实机听读抽样列后续迭代) |
| R24 | PARTIAL | **CLOSED(核心)** | 四套仓库内套件 + 真实 FunASR 套件全部"全新 clone 按文档命令可运行";红-绿过程留档 |

## 5. 停止标准对照(移交文档 §5)

1. R13 实现错误与测试假阳性已修复(红→绿留档)✓
2. R05 两个设置测试接口无安全旁路 ✓
3. R09 额度损坏不会造成未记账的真实调用 ✓
4. R15/R18/R19 达到实际使用标准(见 §2)✓
5. 全部验收脚本可在全新 clone 按文档运行;真实 FunASR 不可达时明确环境阻断 ✓
6. 真实业务链路(上传→转写→分析→说话人标注→重跑→下载)此前轮次已有端到端运行证据,本轮 R09 用例再次覆盖上传→真实转写→分析→完成全链路 ✓
7. 故障链路:外部服务失败(R17 stub)、队列满(503)、状态损坏(额度/高水位 fail-closed)、发布失败回滚(部署脚本自动回滚+回滚镜像保留)✓
8. GitHub=功能提交=生产版本=镜像 digest 四者可追溯(§1)✓
9. 待本轮独立复审确认无新 P0/P1
10. 凭据轮换由用户单独决定并记录,不与代码验收混合 ✓

## 6. 如实说明与后续迭代清单

- 本轮修复过程中发现的额外缺陷一并修复并留证:baseUrl 路径被丢弃(影响带路径接口)、analyzeOnce 对白名单的双重校验失配(与设置接口同源)、regression 测试脚本时区口径。
- 转后续迭代(移交文档 §4):服务端分页/事件推送、跨块语义合并去重、语音级证据对齐、NVDA/VoiceOver 实机抽样、多实例与分布式锁、更大规模容量优化、镜像层 secret scanner CI 强制、nginx 层请求体/速率限制(R18 部署侧配套)。
- 凭据轮换(讯飞/LLM/SSH)仍由用户在外部平台操作后于设置页更新,代码验收不依赖该项。

> 复核建议:先复跑 `node scripts/verify_batch_a.cjs`(167/167,含六项的正/负与故障注入断言),再对真实服务跑 `node scripts/funasr_load_test.cjs`(若本机无 SAPI 语音会明确环境阻断);业务层重点抽——设置页白名单保存后内网模型可测可用、超长会议(>42000 字)中段决议进纪要、额度文件双坏时新任务在付费调用前被拒。

## 附录:复核操作手册(逐条命令与预期输出)

### A. 环境前置

| 项 | 要求 |
|---|---|
| 基线 | `6c9a255`(其后的文档提交不改代码树);全新 clone 即可 |
| Node | ≥ 20(生产为 v22);依赖:clone 后 `npm ci`(测试只用本地 stub,不访问任何真实云服务) |
| Python | 3.9+(仅标准库,无需 pip 安装) |
| ffmpeg/ffprobe | `ffmpeg` 与 `ffprobe` 在 PATH(verify 的媒体探测用例依赖;缺失会导致相应用例失败,属环境问题非代码缺陷) |
| 网络 | 四套仓库内套件全部只连 127.0.0.1 stub;仅 FunASR 压测需要可达真实服务(默认 `http://10.10.10.144:8300`,可用 `FUNASR_URL` 注入) |
| 数据安全 | 全部套件经 `MMB_DATA_DIR` 隔离到临时目录,不触碰任何既有数据;**不要对生产实例做额度损坏类操作**(见 D.3) |

### B. 仓库内四套(全新 clone 直接跑)

**B.1 `node scripts/verify_batch_a.cjs`** — 主验证套件,约 2~5 分钟
预期末行(退出码 0):
```
===== 结果:167 通过,0 失败 =====
```
六个收尾项的代表性实测输出节选(端口号每轮随机):
```
■ R13 三/四轮:分块提取(map/reduce),中段唯一决议可提取且分块原文真实入 prompt
  ok  分块提取:54000 字触发 2 次 map + 1 次 reduce — requests=3 maps=2
  ok  map#1 prompt 含第 1 块真实原文(不含第 2 块标记) — len=… hasMark=false
  ok  map#2 prompt 含中段标记原文(第 900 行) — hasMark=true
  ok  最终结果纳入中段决议(stub 仅按真实原文回传) — ["CHUNK-DECISION-MARK-2 方案…"]
■ R05 三轮/四轮:…(含设置测试接口贯通)
  ok  设置测试:私网域名无白名单 → 400 且提示 allowedLlmHosts
  ok  设置测试:白名单命中 → ok 且 Host 头保留原域名(连接绑定解析 IP) — host=fake-llm.test:<port>
  ok  设置测试:LLM 302 → 不跟随(ok:false),外部目标零命中 — target=0
  ok  旧布尔迁移:磁盘文件已删除 allowPrivateLlmHosts 字段
■ R09 四轮:额度损坏在付费转写前 fail-closed(真流水线 + 讯飞 stub)
  ok  额度健康:当日额度已记账(>0 秒) — 2
  ok  额度损坏:任务明确失败且提示人工恢复
  ok  额度损坏:未发起任何付费调用(stub 请求数不变) — before=N after=N
■ R15 …
  ok  四轮:quote 真实但不在 tref 范围内 → quoteState unverified — ["unverified","verified"]
  ok  四轮:起止颠倒(from≥to)→ trefState invalid — invalid
■ R18 …
  ok  预留占满 → 另一个 HTTP 上传请求被 503 拒绝 — 503
  ok  ffprobe 并发上限 ≤ 2(worker 池生效) — max=2
```

**B.2 `python scripts/regression_batch1.py 8793`** — 回归 30 断言,约 2~4 分钟(含服务重启与并发用例;8793 需空闲)
预期末尾:
```json
"FAILED": []
```
且 `asserts` 中 30 项全为 `true`(R01/R02/R03/R07/LEGACY)。

**B.3 `python tools/local-asr/test_server.py`** — 本地 ASR 服务 stub 全路径,约 1 分钟
预期末行:`===== local-asr 运行期测试:11 通过,0 失败 =====`(日志中坏音频的 ffmpeg 报错为用例预期内容,非环境故障)。

**B.4 `node scripts/browser_matrix.mjs`** — Playwright 浏览器矩阵,约 1~2 分钟
前置:首次运行先 `npx playwright install chromium`(脚本自起隔离 demo 服务,无需手工部署)。
预期末行:`===== 浏览器矩阵:13 通过,0 失败 =====`(覆盖双弹窗 inert/Esc/焦点循环/纯键盘/progressbar ARIA/320px/420px/零 JS 异常)。

### C. 密钥扫描与构建

- `npm run scan:secrets` → 预期 `[scan-secrets] 通过:跟踪 N 个文件,校验 M 个密钥值,无泄漏`。N/M 随本机存在与否浮动(开发机实测 276/4;全新 clone 无密钥文件时 M=0,仍为"通过")。
- `npm run typecheck`、`npm run build` → 均应零错误退出。

### D. 真实环境与生产抽检

**D.1 `node scripts/funasr_load_test.cjs`**(真实 FunASR,默认 10.10.10.144:8300)
预期(实测样例,首轮自动用 SAPI 合成双夹具并缓存,仓库零音频):
```
  ok  健康检查 200 且 ok=true — 7ms model=loaded
  ok  串行转写完成 done — 耗时 2.0s(含排队) error=无
  ok  转写文本非空 — text 119 字
  ok  串行转写内容含第 1 夹具标记词 — [00:00] 说话人0: meeting test one …
  ok  坏音频 → 明确 failed/拒绝(不悬挂) — status=failed
  ok  并发 3 全部 done — 总耗时 6.0s
  ok  并发结果按任务对应(各自含己方标记词) — [{"hasOwn":true,"hasOther":false},…]
===== FunASR 真实压测:12 通过,0 失败 =====
```
环境阻断口径(如实报告,不宣称通过):服务不可达或本机无任何 SAPI 语音时,输出 `[环境阻断] …` 并以**退出码 2** 结束;可用 `FUNASR_WAV=<wav 路径>` 注入外部音频、`FUNASR_URL` 指定服务地址。

**D.2 生产抽检(只读为主,已实测 7/7)**
对 `http://10.20.30.203:8099`:
1. `GET /api/version` → `"commit":"6c9a255"`;
2. `POST /api/auth/login`(凭据由项目方提供)→ 200;
3. `GET /api/settings`(带会话)→ 含 `allowedLlmHosts` 字段、**不含** `allowPrivateLlmHosts`;
4. `GET /api/meta` → `llmConfigured/asrProvider/ffmpeg` 正常;
5. 上传一个假 .wav(文本内容改扩展名)→ **400** `"文件中没有可用的音轨…"`(该请求会自动清理,不产生任务);
6. `GET /api/tasks` → 存量任务完好(实测 7 条,最新 MT-20260921-001 done);
7. `POST /api/settings/test-asr {"localUrl":"http://10.10.10.144:8300"}` → ok:true(guardedFetch 路径)。

**D.3 额度损坏 fail-closed 的业务级复核(务必在隔离实例,勿动生产)**
`MMB_DATA_DIR=<临时目录> MMB_ADMIN_PASSWORD=<口令> node server/server.cjs` 起隔离实例 → 上传音频完成一次真实(或 demo)流程 → 将 `<临时目录>/quota.json` 与 `quota.json.bak` 改为乱文 → 再上传新任务:预期任务**失败**且错误含"额度/人工",讯飞侧无调用(verify B.1 的 R09 节即此场景的自动化版本)。

### E. 常见环境问题

| 现象 | 原因与处理 |
|---|---|
| verify 在媒体探测用例失败 | PATH 无 ffmpeg/ffprobe,安装后重跑 |
| regression 报 port busy | 8793 被占,换一个端口参数 |
| browser_matrix 报找不到浏览器 | 先 `npx playwright install chromium` |
| funasr 套件退出码 2 | 环境阻断(服务不可达/无语音),按输出提示注入 `FUNASR_WAV` 或安装语音后重跑 |
| 全套件中只有依赖真实外网的 funasr 一项受网络影响 | 其余全部本地 stub,离线可跑 |

## 7. F01/F02 修复交付(响应最终复审,2026-09-23)

按最终复审报告 §7 的最小复核范围交付;执行方式仍为"先写失败用例再修实现":在 `6c9a255` 代码上新增断言实跑为 **红:176 通过 / 6 失败**(全部为新断言:partial 未强制 ×3、map 无瞬态重试、模板无警示、HTTP 边界误 503),修复后同套件 **绿:182 通过 / 0 失败**。

### 7.1 版本口径

| 项 | 值 |
|---|---|
| 功能提交(F01/F02 修复,生产运行) | `7e55f94205039e065bb1421144c854a97dbbd8e9` |
| 提交链 | `6c9a255`(第四轮六项收尾)→ `5be0d8c`(文档:交付文档+复核手册)→ `7e55f94`(F01/F02) |
| 生产 `/api/version` | `{"commit":"7e55f94","builtAt":"2026-09-22T22:22:03.857Z","startedAt":"2026-09-22T22:22:34.994Z"}` |
| 生产镜像(不可变 ID,容器实测) | `sha256:399d57ca0430398baf85b5e7d5d72f724e7afad524e6c992153e85cf262b5b81` |
| 回滚镜像(已保留) | `sha256:b01a08bf7f51ec0c000d93843ed41fdc732654fd7a8cff28aa6f61249f9c8944`(= 6c9a255) |
| GitHub `main` | 推送时功能 HEAD = `7e55f94`;本文档提交为之后的纯文档提交(代码树不变,`git diff 7e55f94..HEAD -- server src tools scripts deploy` 为空可证) |

### 7.2 F01(P1)修复与断言

实现(`server/llm.cjs`、`server/minutes-template.cjs`):
1. `analyzeChunked` 维护结构化 `chunkFailures: [{index, reason}]`,不再只把失败写进供 LLM 处理的自然语言;
2. reduce 返回后**强制 `partial=true`** 并挂载 `chunkFailures`——stub 的 reduce 返回全字段(analyzeOnce 自身 partial=false)时也不得覆盖;
3. 新增 `mapChunkWithRetry`:复用与 analyze 相同的瞬态重试策略(失败 3s 后重试一次;`noRetry` 的确定性 4xx 立即失败;重试耗尽进入失败标记);
4. `normalizeAnalysis` 透传 `chunkFailures`(renderMinutes 内部会重新规范化,不透传则警示无法到达模板);纪要模板渲染 `role="note"` 醒目横幅:「第 N 块提取失败,纪要可能缺失,建议重跑分析」——不依赖模型输出,不可被省略,且随 analysis.json 持久化(改名纯重渲染后仍在)。

新增断言(verify R13 节,红→绿):
- 红组a:标记所在块坏 JSON → 决议可丢失,但 `partial=true`、`chunkFailures=[{index:2}]`、模板实际渲染「第 2 块提取失败,纪要可能缺失」(模板断言 `hasBanner=true`);
- 红组b:非标记块空响应 → 即使关键决议仍在,也 `partial=true` 且记录第 1 块;
- 红组c:瞬态 503 一次 → map 复用重试(2 块共 3 次 map 请求),成功后完整交付(`partial=false`、无 chunkFailures、中段决议进结果);
- 红组d:503 重试耗尽 → `partial=true` 且 `chunkFailures` 逐块记录 1,2,决议不伪造;
- 正常路径回归:2 块与 3 块正常用例保持 `partial=false` 且无 `chunkFailures`。

### 7.3 F02(P2)修复与断言

实现(`server/server.cjs`):上传中间件改为**先检查后预留**——`quotaExceeded(cl)`(usage + 在途预留 + 本次)通过后再 `reserveUploadQuota(cl)`;两步之间无 await,进程内原子,消除"先预留后检查"把本次 Content-Length 计算两遍导致的提前 503。

新增断言(verify R18 节):
- 边界契约:`usage+reserved+cl` 恰好等于配额 → 接受;再多 1 字节 → 拒绝;
- 并发防共同越界:余量 10B 时,第一个 6B 上传通过并预留后,第二个 6B 必须被拒;
- HTTP 级边界:余量恰好等于本次上传体积 → 必须 201(修复前实测 503,即评审所述重复计算)。

### 7.4 复跑结果与范围证明

| 项 | 结果 |
|---|---|
| `node scripts/verify_batch_a.cjs` | 红 176/182 → **绿 182/182** |
| `npm run typecheck` / `npm run build` | 通过 |
| 范围 diff(`git diff 6c9a255 --stat`) | 仅 4 文件:`scripts/verify_batch_a.cjs`(测试)、`server/llm.cjs`、`server/minutes-template.cjs`、`server/server.cjs`;**src/(浏览器交互)、tools/(本地 ASR)、server/local.cjs、deploy/ 均未改动**——按评审 §7 第 5 条,真实 FunASR 压测、基础回归与浏览器矩阵无需重做 |
| 保险复跑(超出最低要求) | regression `FAILED: []`(30/30 口径);浏览器矩阵 13/13 |
| 生产抽检(修复后) | 7/7:版本 7e55f94、登录 200、allowedLlmHosts 字段贯通且无旧布尔、test-asr ok:true(guardedFetch 路径)、假音频 400(媒体探测)、7 条存量任务完好、镜像 digest 如 §7.1 |

### 7.5 停止标准对照

F01 已关闭、未引入新 P0/P1(全部断言绿 + 范围限定在评审点名的两个缺陷)——按最终复审报告 §7,满足最终停止标准;F02 一并修复。代理层限制、镜像扫描 CI 门禁、NVDA 抽样、凭据轮换继续按 §6/最终复审 §6 转正常迭代与项目方既定决策。
