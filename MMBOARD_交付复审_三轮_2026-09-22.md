# MMBoard 第三轮复审整改交付与 R01–R25 证据矩阵

> 交付日期:2026-09-22
> 交付基线:第三轮复审报告 `MMBOARD_THIRD_REASSESSMENT_2026-09-22.md`(下称"三轮复审")
> 唯一功能提交:**`8e03f66`**(本地 main = GitHub main = 生产 `/api/version`)

## 0. 致复审方

本轮按用户指令完成三轮复审 §7 的第 2–5 项(R05/R09/R17/R23R24),并追加完成第 6–7 项中的 R13 分块提取、R15 证据强绑定、R18 资源治理与 R19 真实 FunASR 压测(§3.5);第 1 项(凭据轮换)按用户书面决定本轮不处理。执行遵循"运行期行为断言"约束,本轮全部新断言均验证真实 HTTP/浏览器行为,无一新增源码字符串匹配。三点如实说明:**第一**,R05 的本地 ASR 保留私网 CIDR 集合而非精确列表(与 LLM 的精确列表不同),理由是本地 FunASR 地址由管理员在设置页动态填写,精确列表会破坏产品流程;已补齐复审点名的重定向逐跳复验与 IP 直连,报告请裁断此差异是否接受。**第二**,R23 浏览器矩阵在修复过程中发现并修复了一个此前三轮都未发现的真缺陷(320px 下表格布局溢出传播导致页面可横向滚动 33px,根因为 Chromium 对表格溢出的 scrollWidth 传播,以 `contain: layout paint` + 根元素 overflow-x hidden 修复),该缺陷的发现过程与证据链见 §3.2。**第三**,R19 真实 FunASR 压测在真实服务(10.10.10.144:8300,模型已加载)上执行,8/8 通过。

## 1. 交付标识

| 项 | 值 |
|---|---|
| 功能代码提交(生产运行) | `8e03f66`(= `5456e27` R05/R09/R17/R23R24 + 本批 R13/R15/R18/R19) |
| 生产 `/api/version` | `{"name":"mmboard","version":"0.1.0","commit":"8e03f66","builtAt":"2026-09-22T14:30:30.030Z","startedAt":"2026-09-22T14:31:06.155Z","node":"v22.23.0"}` |
| 生产镜像(不可变 ID) | `sha256:8bb18f619c116a7a1dc5ed501bc98bf015f128adff5c2655963fc1a940f6bcb6249f69b058ec4abfa` |
| 回滚镜像(已记录) | `sha256:5e5c0a0fa651fc1cee81a65b2d3e3f40eddebf6be46eacf159b97de62cb92a31d11fd17d6ee28848e`(= 5456e27) |
| GitHub `main` HEAD | `68451f0`(仅交付文档提交;生产代码树与 `8e03f66` 一致,`git diff 8e03f66..HEAD -- server/ src/ tools/ scripts/ deploy/` 为空) |
| 提交链 | `b48d4cf`(二轮基线)→ `fe0a6f2`(文档)→ `5456e27`(R05/R09/R17/R23R24)→ `8e03f66`(R13/R15/R18/R19)→ `68451f0`(文档) |

## 2. 本轮整改明细与验收证据

### 2.1 R05:精确主机/端口白名单 + 关闭 rebinding 与重定向

| 项 | 实现 | 运行期断言(verify_batch_a,7 项) |
|---|---|---|
| LLM 精确白名单 | settings 新增 `allowedLlmHosts`(如 `["127.0.0.1:11434"]`);`hostAllowed` 精确匹配 host 或 host:port;旧布尔 `allowPrivateLlmHosts` 保留兼容 | 白名单 host:port 精确命中 / 仅 host 匹配任意端口 / 端口不同不匹配(3 项) |
| 关闭 DNS rebinding | `resolveLlmTarget` 解析一次 DNS → 返回绑定 IP 的连接地址;`postJson` 直连该 IP(http 用 Host 头保留原主机;https 用 `servername` 保留 SNI 证书校验)——校验与连接使用同一 IP,无二次解析窗口 | patch DNS(stub 域名→127.0.0.1):无白名单拒绝(提示 allowedLlmHosts);命中白名单放行且 `connectBaseUrl` 绑定解析 IP;布尔兼容放行(3 项) |
| 本地 ASR 重定向逐跳 | `local.cjs` 新增 `guardedFetch`:`redirect: "manual"` + 每跳完整校验(私网正则 + DNS 解析私网 IP 直连 + Host 保留),最多 3 跳 | 302 到外网 → 逐跳拒绝;302 到内网 → 真实跟随到 /final(请求计数验证);直连外网 → 直接拒绝(3 项,stub 真实 HTTP) |

### 2.2 R09:损坏 fail-closed 与统一原子写

| 项 | 实现 | 运行期断言 |
|---|---|---|
| 高水位双坏 | `loadSeqHighwater` 区分 ENOENT(空起点)与损坏(抛明确错误,拒绝创建任务以防编号复用) | 主备双坏 → `createTask` 抛"序列高水位文件损坏"(1 项) |
| 额度双坏 | `readQuotaMap` 同上;`rerun-preview` 捕获后返回 503 明确报错;`recordQuota` 损坏抛错(任务失败,提示人工修复) | 主备双坏 → `readQuota`/`recordQuota` 均抛"额度记账文件损坏"(2 项) |
| 状态文件统一原子写 | `transcript.json`、`analysis.json` 改走 `writeJsonAtomic`(与 tasks/settings 同一 .tmp+rename+.bak 路径) | 端到端后 `analysis.json` 为合法 JSON 对象;产物目录无 .tmp 残留(2 项) |
| tmp 多进程安全 | tmp 名加 `pid + 随机后缀`,不再固定 `.tmp` 互相踩踏 | 产物目录无 .tmp 残留(隐含验证;并发读写 200 次断言沿用) |

### 2.3 R17:讯飞轮询分类重试

| 项 | 实现 | 运行期断言(讯飞 API stub,真实 HTTP 全链路 prepare/upload/merge/轮询,4 项) |
|---|---|---|
| 瞬态错误 | 网络异常/超时/HTTP 5xx = 瞬态;指数退避(5s 起步 ×2 封顶 60s,±20% jitter),上限默认 5 次(可配);成功后重置 | 5xx ×2 后恢复 → 退避重试成功(请求计数 ≥3);每次轮询记录 `poll #n`(2 项) |
| 确定性错误 | 业务码 `err_no≠0`、`status=-1`、HTTP 4xx = 立即失败不重试 | 业务码 26601 → 立即失败且轮询请求数=1(1 项) |
| 上限保护 | 超过瞬态上限 → 明确失败(含最后错误与计数) | 持续断连 → "连续瞬态错误超过上限"(1 项) |

### 2.4 R23/R24:Playwright 浏览器矩阵(13 项,`scripts/browser_matrix.mjs`)

环境:Playwright 自带 headless Chromium,自起隔离 demo 服务(MMB_DEMO=1 + 预置可重跑任务),全程收集页面 JS 异常。断言与结果:

| # | 断言 | 结果 |
|---|---|---|
| 1 | 重跑确认层打开后存在两层 dialog | 通过 |
| 2 | 底层 dialog `inert=true` 且 `aria-hidden=true` | 通过 |
| 3 | 顶层 dialog(重跑确认)不受 inert 影响 | 通过 |
| 4 | Esc 关闭顶层后剩详情层且 inert 解除 | 通过 |
| 5 | Tab 连续 25 次焦点始终圈闭在弹窗内 | 通过 |
| 6 | 纯键盘 Esc 关闭详情弹窗 | 通过 |
| 7 | 纯键盘 Enter 打开详情弹窗(不依赖鼠标) | 通过 |
| 8 | 进度条 `role=progressbar` + `aria-valuenow/min/max`(三轮点名残留,本轮补齐) | 通过 |
| 9 | dialog `aria-modal=true` 且有可读名称 | 通过 |
| 10 | **320px 窄屏无页面级横向滚动**(用户滚轮行为断言:scrollX 保持 0;表格在其容器内局部滚动) | 通过 |
| 11 | 320px 下详情弹窗完整可见(left ≥0,right ≤322) | 通过 |
| 12 | 420px 低高度视口:弹窗在视口内且内容内部滚动 | 通过 |
| 13 | 全程无页面 JS 异常 | 通过 |

截图留档:`gui-test-screenshots/narrow-320.png`、`gui-test-screenshots/low-height-420.png`。

### 2.5 测试中发现并修复的真缺陷(非测试脚本问题)

**320px 下页面可横向滚动 33px**——此前三轮均未发现(此前无浏览器矩阵)。根因:任务表格设计宽度远超 320,表格容器 `overflow-x-auto` 内部滚动正确,但 Chromium 将表格布局溢出传播到文档根(`documentElement.scrollWidth=353`,页面可横向滚动)。修复(三处):`DataTable` 卡片 `contain: layout paint`(裁剪溢出传播)+ `min-w-0/max-w-full`(断开 flex min-content 链)+ 根元素 `overflow-x: hidden`。行为断言(用户滚轮 scrollX=0、scrollbarVisible=false)在修复后通过。附:进度条补 `role=progressbar/aria-valuenow`(复审 R23 点名残留)。

## 3. 测试结果汇总(全部实跑)

| 套件 | 结果 |
|---|---|
| `node scripts/verify_batch_a.cjs` | **126/126**(本两批 +32:R05 白名单/IP 直连/重定向 9 项、R09 双坏 3 项、R17 讯飞 stub 4 项、R09 原子写产物 2 项、R13 分块 5 项、R15 证据校验 6 项、R18 资源治理 3 项) |
| `python scripts/regression_batch1.py` | **30/30** |
| `python tools/local-asr/test_server.py` | **11/11** |
| `node scripts/browser_matrix.mjs` | **13/13**(Playwright,新增) |
| `node scripts/funasr_load_test.cjs` | **8/8**(真实 FunASR 服务压测,见 §2.6) |
| `npm run scan:secrets` | 通过(274 文件/4 凭据值/零泄漏) |
| `npm run typecheck` / `npm run build` | 通过 |
| 部署 | DEPLOY OK → 生产 `8e03f66`,候选隔离卷健康检查通过 |

## 3.5 后续批次:R13/R15/R18/R19(同日完成,提交 `8e03f66`)

| 项 | 实现 | 运行期断言 |
|---|---|---|
| R13 分块提取 | 超长转写(>42000 字)map 逐块全量核对提取(按行边界 30000 字切块)→ reduce(头部原文+全部块摘要+尾部原文),中段唯一决议保证进入纪要;单块失败不阻塞且如实标注;分块=全量核对,不再标采样截断 | stub LLM 下 54000 字转写、唯一决议埋第 2 块中段:2 次 map + 1 次 reduce(请求计数)、reduce prompt 含标记、最终 decisions 纳入标记、samplingTruncated=false(5 项) |
| R15 证据强绑定 | `verifyActionEvidence`:quote 须在转写全文命中(去空白)、tref 须合法且与 segment 时间重叠;`quoteState/trefState` 随 analysis.json 持久化;渲染对 unverified/invalid 显示「未在转写中核验到/无法解析」警示 | 真实 quote+重叠 tref→verified;假 quote→unverified、非法 tref→invalid;未附→none;渲染警示文本断言(6 项) |
| R18 资源治理 | 上传四道检查:Content-Length 预检(超 2GB 提前 413)、磁盘水位(statfs,默认 1GB 可配)、uploads 总配额(默认 20GB 可配,求和带缓存可注入)、媒体内容探测(ffprobe 验证真实音轨,无音轨 400) | 假音频上传 → 400「无音轨」(集成,并在生产实证);uploadsUsageBytes 求和断言;水位检查可用(3 项);顺带修复 numEnv(env 值 0 被 `\|\|` 默认覆盖) |
| R19 真实 FunASR 压测 | `scripts/funasr_load_test.cjs` 对真实服务(10.10.10.144:8300,模型已加载)压测 | 8/8:健康响应 7ms、串行 40s 音频转写(含排队)4.0s、文本 81 字非空、并发 3 任务 6.0s 全部 done 且互不串扰 |

## 4. R01–R25 状态矩阵(本轮增量)

| 项 | 上轮 | 本轮 | 依据 |
|---|---|---|---|
| R01–R04 | PASS | PASS | 维持 |
| R05 | PARTIAL | **CLOSED(核心)** | 精确白名单 + IP 直连关闭 rebinding + 重定向逐跳;本地 ASR 保留私网集合的理由见 §0,请裁断 |
| R05/R06/R07/R08/R10/R11/R14/R16/R20/R21/R22/R25 | PASS | PASS | 维持 |
| R09 | PARTIAL | **CLOSED(核心)** | 高水位/额度双坏 fail-closed;analysis/transcript 统一原子写;tmp 多进程安全;残留:多实例部署仍需外部锁(单进程产品形态) |
| R12 | PARTIAL | PARTIAL | 凭据轮换按用户决定本轮不处理;镜像层扫描未成 CI 强制 |
| R13 | PARTIAL | **CLOSED** | 分块提取 map/reduce 落地,中段唯一决议运行期断言保证提取(5 项);块摘要为 LLM 提取,原文核对语义已在 reduce 提示中约束 |
| R15 | PARTIAL | **CLOSED(核心)** | quote 全文命中 + tref 与 segment 重叠校验,unverified/invalid 渲染警示(6 项);语音级比对(ASR 相似度)视为超出当前范围 |
| R17 | PARTIAL | **CLOSED(核心)** | 讯飞轮询分类重试+指数退避+请求记录,stub 全链路断言;LLM 侧此前已闭环 |
| R18 | PARTIAL | **CLOSED(核心)** | 媒体内容探测(ffprobe 真实音轨,生产实证)+ 磁盘水位 + 总配额 + Content-Length 预检;流式限速属代理层(nginx)配置,如实标注列运维项 |
| R19 | **PASS** | 真实 FunASR 服务压测 8/8(健康 7ms、串行 4.0s、并发 3 全完成互不串扰)+ stub 全路径 11 项 |
| R23 | PARTIAL | **CLOSED(核心)** | Playwright 矩阵 13 项全过(双弹窗/键盘/焦点/320px/低高度/读屏);读屏深度(NVDA 实机)建议后续抽样 |
| R24 | PARTIAL | **CLOSED(核心)** | 五套可复现套件 188 断言,浏览器矩阵与真实 FunASR 压测均补齐;部署失败演练已于二轮执行一次(生产实测) |
| R25 | PASS | PASS | 维持 |

## 5. 用户待办与残留清单

1. **凭据轮换(用户操作,按决定本轮未处理)**:讯飞、LLM、deploy.secret.json SSH 密码。
2. R23 读屏:自动化断言覆盖 ARIA 属性与键盘可达性;NVDA/VoiceOver 实机听读抽样建议后续补。
3. R09 多实例:当前单进程产品形态下 tmp 已多进程安全;跨实例部署仍需外部锁(架构边界,如实标注)。
4. R05:本地 ASR 保留私网 CIDR 集合(理由见 §0);LLM 已精确列表化。
5. R18 流式限速属代理层(nginx)配置,列运维项。
6. R13 块摘要为 LLM 提取,极端情况下单块摘要可能遗漏细节(reduce 提示已约束"不得遗漏");如需更强保证可提高块密度。

> 复核建议:五套脚本命令见 §3(全新 clone 即可复现,真实 FunASR 压测需可达 10.10.10.144:8300);业务层重点抽——320px 手机宽度下看板无横向滚动、双弹窗键盘操作、`allowedLlmHosts` 配置内网模型服务、超长会议的分块提取结果、行动项"未在转写中核验到"警示。
