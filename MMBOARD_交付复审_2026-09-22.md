# MMBoard 复审整改交付与 R01–R25 证据矩阵

> 交付日期:2026-09-22
> 交付基线:复审报告 `MMBOARD_REASSESSMENT_2026-09-21.md`(以下称"复审报告")
> 唯一交付提交:**`990a5b7`**(本地 main = GitHub `main` = 生产 `/api/version`,三者一致)
> 生产站点:http://10.20.30.203:8099/

## 0. 致复审方

本轮交付针对复审报告认定的 5 项 FAIL、13 项 PARTIAL 与第 6 节全部 P0/P1 缺陷做了整改。我们在提交复审前想先说清楚三件事,便于贵方确定复核重点。**第一**,本报告的证据分三层:可复现的自动化断言(`scripts/verify_batch_a.cjs` 70 项、`scripts/regression_batch1.py` 27 项,均已在开发机与全新 clone 各跑通一遍)、两次真实部署的日志输出、一轮生产登录态业务复核;凡只有代码而无自动化证据的项目,下表都如实标注。**第二**,少数项目我们有意未按字面验收口径做:R02 的 API 主键仍是展示编号(uuid 用于内部主键与产物目录键)、R13 选择了均匀采样而非 Map-Reduce、R17 未覆盖讯飞轮询瞬态重试——这些取舍的理由写在对应条目里,请贵方裁断是否接受。**第三**,R23 的窄屏/低高度视口、R24 的浏览器矩阵与故障注入测试尚无产出,本轮以代码修复 + 类型检查为界,不宣称完成。以下矩阵中"遗留"标注均为真实残留,不是套话。

## 1. 交付标识

| 项 | 值 |
|---|---|
| 唯一 commit SHA | `990a5b729c823b481f2c91d952841ee8980492a7` |
| GitHub `main` | `990a5b7`(`git ls-remote` 核对一致) |
| 生产镜像(不可变 ID) | `sha256:f0dde46c7b3f72a1c55f3744349367a498b6311a315d425544de849e8c8b62933421d9df75070ec3e` |
| 回滚镜像(上一版,已记录) | `sha256:acbf1ab48b135070458e3c43b86d52d80466d821119f381f9feb10e3d53c7737c38deb61376a9fa16`(7601e14) |
| 生产 `/api/version` | `{"name":"mmboard","version":"0.1.0","commit":"990a5b7","builtAt":"2026-09-22T00:11:20.434Z","startedAt":"2026-09-22T00:11:37.909Z","node":"v22.23.0"}` |
| 提交链 | `f83dfe1`(复审对象)→ `34f4532` → `4c045c1`(批次A/B/C 合入)→ `21027a1` → `af356a7`(性能基线)→ `7601e14`(部署清单修复)→ `990a5b7`(R02/R21/R22/R23) |

## 2. 测试与验证记录

| 验证 | 命令 | 结果 |
|---|---|---|
| 类型检查/构建 | `npm run typecheck` / `npm run build` | 通过(开发机 + 全新 clone) |
| 批次 A 验证 | `node scripts/verify_batch_a.cjs` | **70/70**(开发机 + 全新 clone;隔离数据目录、随机端口、零外部依赖) |
| 核心回归 | `python scripts/regression_batch1.py` | **27/27**(同上;R01/R02/R03/R07/历史兼容 + 本轮新增并发与重启用例) |
| 性能基线 | `node scripts/perf_baseline.cjs` | `perf-baseline.json` 入库(见 §4) |
| 生产部署 | `node deploy/deploy.cjs` | 两次 DEPLOY OK(7601e14、990a5b7),候选健康检查、不可变镜像切换、回滚 ID 记录全部生效 |
| 生产业务复核 | 登录态脚本(7601e14 时执行) | 6 组用例 PASS,详见 §3.1 |
| 生产抽检 | 登录态脚本(990a5b7 后执行) | 5/5 PASS(旧任务 dirKey 回退兼容、settings version 字段、列表完好) |

### 3.1 生产业务复核(7601e14 时执行,测试数据已清理)

1. 登录与配置契约:admin 登录成功;`/api/meta` 讯飞/LLM 判定已配置(新 hasKeys 契约)。
2. R01 同名上传:两个同名 `RECHECK-*.wav` → 存储键不同、原始名相同;删除其一,另一任务文件完好。
3. 真实通道与空结果:正弦波音频走完讯飞 prepare/upload/轮询(密钥有效),空转写明确报错"讯飞转写结果为空",**未静默 mock**(R06)。
4. 全流水线:TTS 语音上传 → 真实转写 49 字 → LLM 分析 → 渲染 37KB 纪要;含「上传时间」「纪要生成」双字段(R15)。
5. speaker 改名 + XSS:说话人0 →「RECHECK-张三」纪要全量替换;注入 `<img src=x onerror=…>` 未以可执行形态出现(R16 生产实证)。
6. 两档重跑:「重新跑分析」transcribe 步骤 *skipped 复用已有转写(未调用讯飞)*,零额度完成。
   附:短转写触发的纪要横幅真实标注「分析未生成章节:title」(R14 生产实证)。

## 3. R01–R25 逐项证据矩阵

结论口径:**CLOSED** = 复审验收目标达成且有可复现证据;**IMPROVED** = 有效整改但仍有明确残留(逐条列出);原文 PASS 项标注维持证据。

| 项 | 结论 | 整改与证据 | 遗留/说明 |
|---|---|---|---|
| R01 同名上传隔离 | CLOSED(复审 PASS 维持) | 回归 R01 组 6 断言(存储键不同/原名相同/双文件在/删除隔离);生产复核第 2 项 | — |
| R02 编号唯一性 | CLOSED(口径见说明) | `createTask` 改为**当日最大序号+1 单调分配,删除空号不回收**;新增 `task.dirKey`(uuid)为**不可复用产物目录键**,transcript/analysis/纪要全部落 `outputs/<uuid>/`,旧任务回退 id(生产抽检第 4 项验证兼容);测试:删除中间不复用(`R02_notReuseDeleted002`)、并发 6 创建全唯一(`R02_concurrent*`)、残留目录占号(`verify` 单元断言 >900)、重启后创建不冲突(`R02_uniqueAfterRestart`) | **有意取舍**:API/查找主键仍为展示编号 `MT-*`,uuid 用于内部主键+目录键。理由:全量切换 API 主键波及前端与 7 条存量任务,风险大于收益;冲突风险已由"不回收+uuid 目录键"消除。请复审方裁断 |
| R03 认证与授权 | CLOSED(核心) | auth.json 损坏 **fail-closed 拒绝启动**(启动预检 `process.exit(1)`);删除公开默认密码→一次性随机密码/`MMB_ADMIN_PASSWORD` 注入;登录限流 5 次/分钟/IP(429 断言);审计清洗控制字符+不信任 X-Forwarded-For(断言);status 与中间件同一 `resolveSession`(登出后一致);会话密钥改密轮换 | 单管理员模型维持(内网工具定位);普通用户/对象级授权不在本轮范围 |
| R04 产物访问保护 | CLOSED(复审 PASS 维持) | 部署后匿名复核:任务 401/纪要 401/旧 `/outputs` 404 | 复审建议的 `Cache-Control: no-store` 未加(建议项,非验收) |
| R05 SSRF/密钥换主机/测试接口 | CLOSED(核心) | **LLM 地址白名单** `assertLlmUrl`:仅 http/https、DNS 解析后拒私网/保留地址(默认)、云元数据无条件拒、`allowPrivateLlmHosts` 显式放行内网模型(8 项断言);`/api/settings/test` 限流(429 断言);baseUrl 变更拒沿用打码密钥(原有);响应 10MB 上限+总截止 25min | 本地 ASR 的 fetch 重定向未逐跳复验(目标为受控内网 FunASR 组件,风险评级低,如实标注) |
| R06 模拟模式标识 | CLOSED | mock 仅在显式 demo 开关(`MMB_DEMO=1`/配置 `demo:true`),且 **demo 优先于真实配置**;生产缺配置明确抛错(断言);生产复核第 3 项实证(空转写报错非 mock);纪要持续横幅标注 mock/采样/partial/缺失章节 | transcript.json 内嵌 mode 未做(证据链靠任务+analysis 元数据,如实标注) |
| R07 运行互斥 | CLOSED(复审 PASS 维持) | 回归 R07 组:二次 restart 409、运行中删除 409、CSRF 403 | — |
| R08 队列与重启恢复 | CLOSED | 上传/重跑**在改写任何状态前**检查队列满(503,不产生永久卡 queued;源码顺序断言);`enqueuePipeline` 满同步抛错(断言);重启恢复中断任务(原有) | "队列恰好满"的 HTTP 级时序测试不稳定,以单元+源码顺序断言替代(如实说明) |
| R09 原子持久化 | CLOSED | `server/persist.cjs` 统一原子写(.tmp+rename+.bak)与容错读(.bak 恢复+`.corrupt` 隔离+双坏抛错,5 断言);任务/设置/认证/额度全走该模块;**Windows 瞬态锁修复**:rename 遇并发读句柄 EPERM 自动重试(`renameWithRetry`,并发读写 200 次断言)——该缺陷曾在实测中致任务状态写失败永久卡 queued | — |
| R10 部署清单/版本接口 | CLOSED(复审 PASS 维持) | 打包清单补 `server/persist.cjs`(7601e14,部署实证);`/api/version` 回显 commit | — |
| R11 候选/健康/回滚 | CLOSED(核心) | 候选与生产均以**不可变 imageId(sha256)** 运行;回滚依据旧容器 `.Image` ID;健康检查逐项 `curl -sf`+2xx 正则+响应体含部署 commit;候选挂生产数据卷;远端解包先清理;两次部署日志为证 | 失败回滚演练未真实触发(路径已不可变化,如实标注) |
| R12 密钥/传输/容器权限 | IMPROVED | secret 不进镜像/归档(维持);auth.json 原子写+0600;**发现并堵住**:`server/meeting.secret.json.bak` 曾不在 .gitignore,已补规则并确认从未入库 | 镜像层/归档的自动化 secret 扫描未建;内网 HTTP/root 容器按复审书面决策不扣分 |
| R13 长会采样 | IMPROVED | 中段**按行均匀采样保留 ~12000 字**(不再整体丢弃),覆盖率随提示与横幅标注;单测:中段标记必被采到、覆盖率<100 | **有意取舍**:Map-Reduce/分块摘要未做,均匀采样为中段覆盖的务实缓解(采样率约 20–30%,非全文)。请复审方裁断 |
| R14 输出结构校验 | CLOSED | `missingFields` 区分缺失/空/类型错→`partial=true`;横幅明示「分析未生成章节:…」不再伪装"无";生产实证(短转写缺 title 被标注) | — |
| R15 日期语义/追溯 | CLOSED(核心) | 纪要分立「上传时间」「纪要生成」双字段(生产实证),会议日期明确标注"未提供"不冒充;行动项新增 `tref` 时间范围,无 quote 时标注「模型推断,未附原文」 | 行动项与 transcript segment 的强绑定(自动校验引用真实性)未做 |
| R16 speaker 转义 | CLOSED | tooltip 使用预编码 `speakerHtml`(源码+渲染产物断言);恶意 speaker 渲染断言;**生产注入实证 PASS**(7601e14 复核第 5 项) | — |
| R17 外部调用边界 | IMPROVED | LLM:总截止 25min+空闲超时+响应 10MB 上限+close 清理;4xx 确定性错误不重试、空正文自动重试一次(原有);讯飞单请求 30s 超时 | 讯飞轮询瞬态错误仍可使任务失败;统一外部调用器(指数退避/jitter/attempt 记录)未建 |
| R18 上传生命周期 | IMPROVED | fileFilter 早期+2GB 上限+失败清理(原有);队列满前置拒绝;上传限流 6 次/分钟(实测在回归中真实拦截并发上传) | 媒体内容探测、代理层流式限速、总存储配额/磁盘水位未做 |
| R19 本地 ASR | CLOSED | `tools/local-asr/server.py` 重写:转码与识别全部进 worker 线程(async 路径零阻塞,源码断言);有界队列 8,满则 503;queued/running 分离;结果与临时文件 TTL(2h)周期清理 | 端到端验证需 FunASR 运行环境,本轮为静态断言级(如实标注) |
| R20 详情随轮询更新 | CLOSED(复审 PASS 维持) | 机制未变;本轮顺带为设置保存增加了 meta 即时刷新 | 上传/重跑的 2s 快轮询叠加与降频未做(复审建议项) |
| R21 设置草稿/测试 | CLOSED | `testDraftForm` 补 catch(网络异常明确 toast,断言在类型层);保存失败草稿保留(原有);**保存后广播事件,看板立即刷新通道状态**(事件监听,不等轮询) | — |
| R22 字段保留/并发编辑 | CLOSED | settings 增加 `version`:GET 返回、PUT 回传,不匹配 **409** 明确提示"已被其他窗口修改"(3 断言);未回传 version 的旧客户端放行(兼容说明);额度跨日改**本地自然日**(`localDay` 断言) | asr 轻量切换端点未纳入 version 校验(仅改 asr 单字段,冲突面小,如实标注) |
| R23 弹窗焦点/尺寸/多层 | IMPROVED | 多层弹窗:重跑确认层打开时详情层 **inert+aria-hidden**,被覆盖层不响应 Esc/Tab、不抢焦点、不归还触发焦点(组件级修复+类型检查通过) | 320px/低高度视口与读屏的浏览器回归证据未产出(如实标注,建议列入浏览器矩阵) |
| R24 核心回归测试 | IMPROVED(接近 PASS) | `scripts/regression_batch1.py` 修复路径/认证/进程探测/断言口径(复审 2.3 四项全改),27 断言;新增 `scripts/verify_batch_a.cjs` 70 断言(认证/XSS/契约/SSRF/persist/队列/R02/R22);**开发机+全新 clone 双环境全过**,零绝对路径 | 浏览器矩阵、provider 故障注入(stub 429/503/断流)、部署失败演练仍缺(如实标注) |
| R25 性能基线 | CLOSED | `perf-baseline.json`:100/1,000/10,000 任务三档,p50 11.5ms/66ms/576ms,响应体 115KB/1.15MB/11.6MB,全量写 3.5/11.7/85ms,event-loop lag 15.8/71.6/283ms;基线冻结 | 分页/摘要/增量事件等优化按复审建议在基线冻结后另行评估 |

## 4. 复审报告第 6 节缺陷清单关闭状态

| 优先级 | 缺陷 | 状态 |
|---|---|---|
| P0 | R11 回滚保存可变 `:cand` 标签 | **关闭**(不可变 imageId,两次部署实证) |
| P0 | R16 ECharts tooltip DOM XSS | **关闭**(speakerHtml 预编码+生产注入实证) |
| P0 | auth.json 损坏恢复公开默认密码 | **关闭**(fail-closed,公开默认密码已删除) |
| P1 | 讯飞 UI 字段与适配器不一致 | **关闭**(统一 schema+secretKey 别名+生产契约复核) |
| P1 | 重跑先写 queued 队列满不回滚 | **关闭**(前置拒绝,源码顺序断言) |
| P1 | 说话人名称固化进分析结果 | **关闭**(提示词约束 person=说话人N+展示层映射;生产改名实证全量替换) |
| P1 | speaker PUT 无运行互斥/绕过原子写 | **关闭**(409 互斥+writeJsonAtomic,断言) |
| P1 | R19 本地 ASR 未整改 | **关闭**(worker/有界队列/TTL) |
| P1 | LLM 地址无允许规则/测试无限流 | **关闭**(assertLlmUrl+限流) |
| P1 | 原子写只覆盖部分路径 | **关闭**(persist 模块统一+EPERM 重试) |
| P2 | 生成日期仍用上传日期 | **关闭**(双字段分立,生产实证) |
| P2 | 残缺 LLM JSON 被标完整并显示"无" | **关闭**(missingFields→partial→横幅,生产实证) |
| P2 | 审计日志控制字符注入 | **关闭**(清洗+XFF 不信任,断言) |
| P2 | GitHub/本地/生产版本不一致 | **关闭**(`990a5b7` 三方一致,`git ls-remote`+`/api/version` 核对) |

## 5. 已知残留汇总(供复审方列为下一轮范围)

1. R13 采样为均匀采样缓解,非 Map-Reduce 全文覆盖;
2. R17 讯飞轮询瞬态错误仍可致失败,统一外部调用器未建;
3. R18 媒体内容探测、流式限速、存储配额未做;
4. R23 窄屏/读屏证据、R24 浏览器矩阵与 provider 故障注入测试未产出;
5. R12 自动化 secret 扫描未建;R04 `no-store` 建议未加;
6. R02 API 主键为展示编号(uuid 为内部主键+目录键)的取舍待复审方认可。

> 复核建议:先跑 `node scripts/verify_batch_a.cjs` 与 `python scripts/regression_batch1.py`(全新 clone 即可,详见脚本头注释),再对照本文 §1 核对生产 `/api/version`;业务层重点抽 R02 并发创建、R22 双窗口并发保存 409、R16 恶意 speaker 三项。
