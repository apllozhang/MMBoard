# MMBoard 独立代码评审与整改建议

> 评审角色：独立评审方，仅提出问题和验收要求，不参与代码建设。  
> 评审对象：MMBoard 当前工作区及部署站点 `http://10.20.30.203:8099/`。  
> 评审日期：2026-09-20。  
> 本报告所述线上检查均为只读检查；未上传、删除、重跑任务或修改配置。

## 1. 结论与整改顺序

当前应先处理数据串用、匿名访问、并发执行冲突和部署不可复现问题。这些问题可能导致会议文件被覆盖、纪要归属错误、原始转写泄露，以及重新部署后服务无法启动。

建议按以下顺序整改：

1. 修复文件存储、任务 ID 和运行实例冲突。
2. 加入身份认证、权限控制，关闭转写文件的静态公开。
3. 修复部署清单和发布回滚流程。
4. 完善任务队列、持久化、取消、恢复和资源限制。
5. 改进长会议分析、LLM 输出校验及结果可追溯性。
6. 修复前端轮询、设置保存和弹窗交互问题。
7. 为每个问题补充有针对性的回归测试。

优先级说明：

- **P1**：影响数据正确性、安全、部署或主要功能，应优先修复。
- **P2**：影响异常处理、性能、体验和维护成本。
- **P3**：长期维护改进。

## 2. P1：数据正确性与访问安全

### R01：同名上传会覆盖并串用源文件

**位置：** `server/server.cjs:97-103`、`server/pipeline.cjs:144-166`、`server/server.cjs:335-337`

上传文件直接使用清洗后的原始文件名。两个任务上传同名文件时，会指向同一存储路径。后上传文件可能覆盖先上传文件；旧任务重跑时可能处理另一场会议；删除其中一个任务还会删除另一个任务引用的文件。

**整改建议：**

- 为每次上传生成唯一内部存储键。
- 分开保存 `originalFileName` 和 `storageKey`。
- 原文件、转码文件、转写结果按任务或运行实例独立存储。
- 删除时只清理该任务明确拥有的文件。

**验收：** 上传两个内容不同但名称相同的文件，存储路径不同；各自重跑内容正确；删除其中一个不影响另一个。

### R02：删除任务后可能生成重复任务 ID

**位置：** `server/pipeline.cjs:38-64`

任务编号使用“当天现存任务数 + 1”。例如已有 `001、002、003`，删除 `002` 后，新任务会再次得到 `003`。后续查询、状态更新、删除和输出目录均可能指向错误任务。

**整改建议：**

- 内部主键改用 UUID 等不可重复标识。
- `MT-日期-序号` 如需保留，仅作为展示编号。
- 不要只改成“最大编号 + 1”，该方式仍需解决并发和多实例竞争。
- 上线前增加历史重复 ID 检查；若需要修复历史数据，应先提交迁移方案并征求用户同意。

**验收：** 删除中间记录、删除最后记录、并发创建和服务重启后创建，均不得产生重复主键或共享输出目录。

### R03：所有业务 API 缺少认证和授权

**位置：** `server/server.cjs:105-346`

线上只读检查确认，未携带登录凭据即可读取 `/api/meta`、`/api/tasks` 和 `/api/settings`。上传、设置修改、重跑和删除接口也没有看到认证或授权中间件。

**整改建议：**

- 明确普通查看者、会议上传者、任务所有者和管理员的权限边界。
- 所有业务 API 统一接入身份认证。
- 设置修改仅允许管理员；读取、重跑和删除按任务归属授权。
- 下载及静态产物使用同一套权限规则。
- 使用 Cookie 会话时增加 CSRF 防护。
- 删除、重跑和设置修改记录操作者、时间及结果。

**验收：** 匿名用户不能读取任务或设置；普通用户不能修改模型配置；直接构造任务 ID 不能越权读取或删除任务。

### R04：原始转写文件被静态公开

**位置：** `server/server.cjs:341-342`、`server/pipeline.cjs:192-198`

`transcript.json` 与纪要 HTML 位于同一输出目录，而整个目录通过 `express.static` 发布。线上只读 HEAD 请求已确认某任务的 `transcript.json` 返回 HTTP 200。

**整改建议：**

- 分开存放原始转写、中间产物和可下载纪要。
- 移除整个工作目录的静态公开。
- 通过受权限保护的接口下载明确允许访问的文件。
- 若提供分享功能，使用可过期、可撤销的独立分享凭证。

**验收：** 匿名访问原始转写和纪要均被拒绝；猜到目录和文件名也不能绕过权限。

### R05：可编辑服务地址可能造成 SSRF 或模型密钥外送

**位置：** `server/server.cjs:139-207`、`server/server.cjs:225-239`

服务端接受任意 `baseUrl` 和 `localUrl` 并主动请求。编辑模型时，打码密钥会自动替换为旧明文密钥。因此能够修改设置的人可更换请求目标，再让服务器将旧密钥发送至新地址。

**整改建议：**

- 首先落实 R03 的管理员权限。
- 对协议、域名和端口建立明确允许规则。
- 修改目标主机时要求重新输入密钥，不自动沿用旧主机凭据。
- 本地 ASR 的内网地址使用单独的允许范围。
- 校验重定向后的目标，并配置服务端出站访问限制。
- 测试接口增加频率限制和审计。

**验收：** 非管理员不能测试设置；不允许的地址被拒绝；修改主机后旧密钥不会发送给新主机。

### R06：模拟转写标记丢失，可能输出未标注的虚构纪要

**位置：** `server/iflytek.cjs:76-80`、`server/pipeline.cjs:175-198`、`server/minutes-template.cjs:169`

讯飞未配置时会返回固定示例转写。流水线得到 `trMock` 后没有持久化或传到最终模板。若 LLM 配置真实可用，最终分析会被标记为非 mock，导致示例会议内容看起来像真实纪要。

**整改建议：**

- 生产模式缺少必要配置时明确失败。
- 演示模式必须显式开启。
- 分别保存 `transcriptionMode`、`analysisMode`、供应方和配置版本。
- 任一环节使用模拟数据时，页面和下载文件都持续显示醒目标识。
- 移动端不得隐藏模拟数据标识。

**验收：** 覆盖“真实/模拟转写 × 真实/模拟分析”四种组合，任何含模拟环节的产物均不能显示为普通真实纪要。

## 3. P1：任务执行和部署可靠性

### R07：同一任务可并发重跑，运行中删除不会停止后台工作

**位置：** `server/server.cjs:288-338`、`server/pipeline.cjs:115-245`

重跑没有检查任务是否正在执行，也没有运行实例 ID。旧执行和新执行会同时写同一任务状态、转码路径及输出目录。删除任务只删除记录和文件，后台任务仍可能继续调用转写和 LLM，并重新生成文件。

**整改建议：**

- 每次执行生成独立 `runId`。
- 同一任务只允许一个有效运行实例，重复重跑返回 HTTP 409。
- 状态更新必须核对 `runId`，旧执行不能覆盖新执行。
- 增加取消机制，并传递给 HTTP 请求、轮询和 ffmpeg 子进程。
- 运行中删除应先取消并等待完成，或直接拒绝。

**验收：** 两个客户端同时重跑时只有一个请求成功；任务删除后不再产生新文件；旧运行结束不会覆盖新结果。

### R08：没有真实任务队列和服务重启恢复机制

**位置：** `server/server.cjs:249-253`、`server/pipeline.cjs:115-139`

上传后立即启动流水线，没有阶段并发限制。服务重启后也没有恢复或标记未完成任务的逻辑。

**整改建议：**

- 设置排队容量以及转码、转写、分析的独立并发上限。
- 持久化运行实例、外部任务 ID、阶段检查点和心跳。
- 启动时识别失去执行者的任务，标记中断或按检查点恢复。
- 外部提交及可能产生费用的请求必须考虑幂等。

**验收：** 批量提交不会突破并发上限；分别在各阶段重启服务，任务可恢复或明确显示中断，不会永久显示运行中。

### R09：JSON 文件直接覆盖，异常退出可能损坏全部状态

**位置：** `server/pipeline.cjs:33-34`、`server/server.cjs:37-44`

任务、设置和额度均直接覆盖 JSON 文件。设置读取失败会静默退回空配置，无法区分首次运行和文件损坏。

**整改建议：**

- 短期集中封装存储层，使用同目录临时文件、完整写入、原子替换及备份。
- 区分文件不存在、JSON 损坏和权限错误。
- 写入失败时返回明确错误，不得继续报告操作成功。
- 若准备使用数据库，先提交数据结构、迁移和回滚方案；数据库操作必须先征求用户同意。

**验收：** 模拟磁盘写失败、异常退出和 JSON 损坏后，旧数据仍可恢复，且配置不会静默清空。

### R10：部署清单遗漏启动依赖和图表资源

**位置：** `deploy/Dockerfile:8-13`、`deploy/deploy.cjs:52-58`、`server/pipeline.cjs:13`

仓库中存在 `server/local.cjs` 和 `vendor/echarts.min.js`，但部署打包和 Docker COPY 清单均未包含。`pipeline.cjs` 无条件加载 `local.cjs`，因此干净镜像可能无法启动；ECharts 缺失则使图表静默降级。

**整改建议：**

- 修复构建上下文和 COPY 清单。
- 减少多个位置手工维护同一文件列表。
- 在干净环境构建并启动容器。
- 增加 `/api/version`，返回版本、提交号和构建时间，不包含密钥。

**验收：** 仅依赖仓库、锁文件及外部配置即可构建启动，不依赖开发机补丁文件。

### R11：部署先删除旧容器，失败时会直接中断服务

**位置：** `deploy/deploy.cjs:85-114`

脚本在新镜像完成构建和健康检查前删除旧容器。构建命令通过管道交给 `tail`，如果没有正确传播管道退出状态，还可能掩盖构建失败。

**整改建议：**

- 先构建不可变版本镜像并启动候选实例。
- 候选实例通过 API 和版本健康检查后再切换流量。
- 保留旧版本用于快速回滚。
- 正确检查构建命令的退出码。
- 健康检查应覆盖 API、运行依赖和版本，不能只检查静态文件 HTTP 200。

**验收：** 主动制造构建或启动失败时旧服务仍可用；成功发布后版本接口显示预期提交号。

### R12：密钥进入镜像，生产入口仍使用 HTTP

**位置：** `deploy/Dockerfile:10`、`deploy/deploy.cjs:57`

`meeting.secret.json` 被放入部署包并 COPY 到镜像。用户提供的部署入口为 HTTP。

**整改建议：**

- 密钥通过运行时只读挂载或受控 secret 机制注入，不写入镜像层。
- 设置文件限制操作系统访问权限。
- 入口启用 HTTPS，HTTP 自动跳转 HTTPS。
- 容器使用非 root 用户并配置 CPU、内存和磁盘限制。
- 根据旧镜像和部署包的实际接触范围评估是否需要轮换密钥。

**验收：** 镜像和部署归档不含真实密钥；浏览器提交密钥使用 HTTPS；容器只具备必要权限。

## 4. P1/P2：纪要内容质量和外部调用

### R13：长会议直接丢弃中段，且截断边界可失效

**位置：** `server/llm.cjs:84-119`

超过 42,000 字符时只保留开头和结尾，中段决议和行动项会被遗漏。`headCut` 从目标位置向后查找换行且没有距离上限；隔离测试中，100,001 字符输入生成的提示反而超过 100,000 字符，截断基本失效。

**整改建议：**

- 短期设置严格字符或 token 上限，并标记内容未完全覆盖。
- 长期按时间或议题分块提取，再分层汇总和去重。
- 决议、行动项保存来源片段和时间范围。
- 保存分析覆盖范围和被省略范围。

**验收：** 唯一关键决议位于长会议中段时仍能提取；超长单行输入不会突破预算；不完整结果不能冒充完整纪要。

### R14：只校验 JSON 语法，不校验 LLM 输出结构

**位置：** `server/llm.cjs:149-170`、`server/minutes-template.cjs:34-167`

例如 `{"title":"x","topics":"invalid"}` 会通过 JSON 解析，但模板随后因 `topics.map` 不存在而失败。截断 JSON 被修复后也没有标记为部分结果，缺失字段可能被显示为“无”。

**整改建议：**

- 定义运行时 Schema，校验所有字段和嵌套类型。
- 检查模型返回的 `finish_reason` 或 `stop_reason`。
- 明确区分完整、部分和失败结果。
- 必需字段缺失时不得渲染成“无”。
- 结构通过校验后才能进入模板。

**验收：** 错误字段类型、空对象、数组中的 `null`、字段缺失及输出截断均不会导致崩溃或生成伪完整纪要。

### R15：会议日期实际使用上传日期，内容缺少证据追溯

**位置：** `server/pipeline.cjs:235-241`、`server/minutes-template.cjs:337-345`

模板中的“会议日期”来自任务创建日期。上传历史录音时日期会错误，相对期限也缺少基准。当前没有保存完整结构化分析、模型版本、提示词版本及引用片段。

**整改建议：**

- 分开保存会议发生时间、上传时间、生成时间和时区。
- 会议时间未知时明确显示未知。
- 保存结构化分析、模型及提示词版本。
- 决议、行动项和点名点评附来源片段或时间范围。
- 允许人工确认说话人编号与真实姓名的对应关系。

**验收：** 历史录音不再把上传日显示为会议日；行动项可追溯到转写原文；未确认的人名有明确标识。

### R16：发言统计摘要中的 speaker 未做 HTML 转义

**位置：** `server/minutes-template.cjs:131-150`

模板多数位置使用 `esc()`，但发言统计摘要直接插入 `x.speaker`。隔离测试已确认 HTML 标记会原样进入最终文档。

**整改建议：**

- 所有进入 HTML 文本和属性的值统一经过转义。
- 转写适配层限制 speaker 的格式和长度。
- 检查 ECharts tooltip 等字符串拼接点。
- 对生成文档设置适当的内容安全策略或使用隔离产物域名。

**验收：** speaker 中的 HTML、引号和特殊字符只作为普通文字显示，不执行任何脚本。

### R17：外部服务超时、响应大小和中断处理不完整

**位置：** `server/llm.cjs:13-31`、`server/iflytek.cjs:31-57`、`server/local.cjs:47-70`

LLM 只有 socket 空闲超时，没有总截止时间和响应大小上限；响应中断缺少完整处理。讯飞单次请求没有明确超时。本地转写一次轮询失败就终止整个任务。

**整改建议：**

- 统一外部请求封装，提供连接超时、空闲超时、总截止时间、响应上限和取消信号。
- 区分可重试与不可重试错误。
- 轮询采用有限次数的退避重试。
- 对提交、计费请求先设计幂等再重试。

**验收：** 模拟断流、慢响应、429、503 和单次轮询超时，任务能在规定时间内成功、明确失败或取消。

## 5. P2：上传、本地转写和界面

### R18：上传文件生命周期和资源控制不完整

**位置：** `server/server.cjs:97-103`、`server/pipeline.cjs:141-203`

不支持的文件先落盘后才报错；没有请求频率、总存储或队列容量限制；删除任务未统一清理转码文件和失败任务输出目录；本地和讯飞适配器均会把整个音频读入内存。

**整改建议：**

- 上传先进入独立临时区，检查类型、内容及音轨后再接收。
- 统一维护任务文件清单和清理流程。
- 使用流式或分块读取，避免整文件进入内存。
- 加入容量、速率、磁盘阈值和保留期限。
- 大文件上传增加进度和取消。

**验收：** 无效文件、中断上传、失败或删除任务不留下孤立文件；并发上传和内存占用受控。

### R19：本地 ASR 在异步接口中执行阻塞转码

**位置：** `tools/local-asr/server.py:148-168`

异步请求中直接执行同步 `subprocess.run`，转码期间可能阻塞事件循环。队列无容量限制，结果永久保存在内存，排队任务也被标记为 `running`。

**整改建议：**

- 将转码放到工作线程或独立 worker。
- 限制上传大小、排队数量和执行并发。
- 区分 `queued`、`converting`、`running`、`done` 和 `failed`。
- 结果设置保留期限，并按恢复要求选择持久化方案。
- 增加服务间认证和取消接口。

**验收：** 转码期间健康检查仍能及时响应；超过队列容量时明确拒绝；结果按期清理。

### R20：任务详情弹窗不会随轮询更新

**位置：** `src/pages/MeetingBoardPage.tsx:41-68`、`src/pages/MeetingBoardPage.tsx:256-264`、`src/pages/MeetingBoardPage.tsx:422-480`

详情状态保存的是打开时的整条任务对象。列表轮询更新后，详情仍可能显示旧阶段、旧步骤和旧按钮。轮询请求也可能重叠，多个快速轮询定时器会叠加。

**整改建议：**

- 只保存 `detailTaskId`，从最新任务列表派生详情。
- 前一次轮询完成后再安排下一次。
- 使用取消信号或请求序号拒绝过期响应。
- 没有运行任务或页面不可见时降低轮询频率。
- 显示最后更新时间和连接异常状态。

**验收：** 保持弹窗打开直到任务完成，无需重新打开即可看到最新结果；延迟响应不能让状态倒退。

### R21：模型保存失败仍关闭草稿，草稿测试可能测到旧配置

**位置：** `src/components/ModelSettings.tsx:59-117`

`persist()` 捕获错误后没有返回失败标识，因此 `.then(() => setDraft(null))` 仍会执行。编辑已保存模型且未修改打码密钥时，测试接口只接收条目 ID，可能测试旧地址和旧模型，而不是当前草稿。

**整改建议：**

- 保存函数明确返回成功或失败，仅成功后关闭草稿。
- 草稿测试必须使用当前表单中的地址、协议和模型。
- 旧密钥应通过受控凭据引用使用；变更目标主机时要求重新输入。
- 为测试流程补充完整异常提示。
- 设置保存成功后刷新首页元数据。

**验收：** 保存失败时输入仍保留；修改模型或地址后测试使用新草稿；网络异常有明确提示。

### R22：设置整份覆盖会丢弃独立字段并产生编辑冲突

**位置：** `server/server.cjs:17-20`、`server/server.cjs:139-162`

代码支持 `asrDailyQuotaSeconds`，但保存设置时只写 `activeId`、`models` 和 `asr`，可能丢弃额度字段。多个浏览器同时编辑也会整份覆盖。

**整改建议：**

- 建立完整配置 Schema。
- 拆分模型、ASR 和额度设置的更新接口或更新边界。
- 使用配置版本号或 ETag 检测并发编辑冲突。
- 额度日期按供应商规则和配置时区计算。

**验收：** 修改模型不会丢失额度设置；并行编辑产生明确冲突提示；跨日统计口径稳定。

### R23：弹窗焦点可能因轮询重置，长内容缺少视口约束

**位置：** `src/components/Dialog.tsx:19-59`

焦点 effect 依赖 `onClose`，而调用方常传入内联函数。页面轮询重新渲染时可能重新执行焦点逻辑。弹窗也没有最大高度及内部滚动区域，小屏或低高度窗口可能难以到达底部按钮。

**整改建议：**

- 焦点初始化只绑定弹窗从关闭到打开的状态变化。
- 使用稳定关闭回调或 ref 获取最新回调。
- 增加最大视口高度、内容区滚动及背景滚动锁定。
- 嵌套弹窗只由最上层管理焦点。

**验收：** 在弹窗内输入并等待多次轮询，焦点不跳走；320px 窄屏和低高度窗口中所有操作均可到达。

## 6. P2/P3：测试与可维护性

### R24：现有验证脚本缺少核心业务回归测试

**位置：** `scripts/verify.mjs`

当前脚本主要检查页面渲染和少量交互。“键盘排序”实际使用点击；分页只检查存在当前页标记；详情弹窗选择器可能打开设置弹窗。测试依赖相邻仓库中的浏览器依赖，且没有覆盖本报告的关键业务风险。

**整改建议：**

| 层级 | 必须覆盖 |
|---|---|
| 单元测试 | ID 唯一性、文本分块、LLM Schema、HTML 转义、mock 来源传播 |
| API 集成测试 | 权限、同名上传、重复重跑、运行中删除、配置冲突 |
| 故障测试 | 断网、限流、断流、磁盘写失败、服务重启 |
| 部署测试 | 干净构建、缺依赖、失败回滚、版本一致性 |
| 浏览器测试 | 详情实时更新、保存失败保留输入、草稿测试、键盘及小屏弹窗 |

测试必须使用隔离临时目录或隔离测试服务，不得操作生产会议数据。

### R25：全量读取、全量写入和全量轮询会随数据增长变慢

**位置：** `server/server.cjs:23-28`、`server/server.cjs:242`、`server/pipeline.cjs:33-34`、`src/components/DataTable.tsx:92-100`

每次列表请求读取完整任务 JSON，并逐项同步检查文件；前端每十秒获取全部任务；每次步骤更新重写完整任务文件。

**整改建议：**

- 列表 API 支持服务端分页、排序和过滤。
- 列表只返回摘要，详情单独获取。
- 减少对历史任务的同步文件检查。
- 运行任务使用增量更新。
- 建立代表性任务规模下的性能基线，再决定是否迁移存储。
- 明确任务和产物保留策略。

**验收：** 在代表性数据量下记录列表响应、状态写入、内存和磁盘占用，形成可重复比较的基准。

## 7. 开发 AI 的工作要求

开发 AI 应按以下规则执行：

1. 按整改批次工作，不要一次性重写整个项目。
2. 每批修改前说明计划、涉及文件、风险和验收方法。
3. 先复现问题，再修改，再提供验证证据。
4. 不得在生产站点执行上传、删除、重跑、设置修改或破坏性测试。
5. 不得随意删除历史任务、上传文件或纪要产物。
6. 涉及数据库引入、数据库操作或历史数据迁移，必须先询问用户。
7. 每批完成后列出所有修改文件，并说明每个文件为什么修改。
8. 构建和类型检查通过不等于业务问题已解决，必须执行对应验收用例。
9. 若整改过程中发现报告假设与当前代码不符，应提供代码证据并更新实施方案，不要机械照抄建议。
10. 优先保持现有部署可回滚，任何上线操作应单独取得用户授权。

建议拆分为以下批次：

| 批次 | 内容 |
|---|---|
| 1 | R01、R02、R07：数据隔离、唯一 ID、运行实例互斥 |
| 2 | R03、R04、R05、R12、R16：访问控制、密钥与输出安全 |
| 3 | R10、R11：可复现部署、健康检查和回滚 |
| 4 | R06、R08、R09、R17、R18、R19：任务可靠性和资源控制 |
| 5 | R13、R14、R15：长会议、结构校验和可追溯性 |
| 6 | R20、R21、R22、R23：前端状态及设置体验 |
| 持续 | R24、R25：回归测试和性能基线 |

## 8. 评审验证记录

- TypeScript `tsc --noEmit` 检查通过。
- 隔离内存测试确认删除中间任务后可能复用现有任务 ID。
- 隔离测试确认特殊换行分布可使长文本截断失效。
- 隔离测试确认语法合法但结构错误的 LLM JSON 会在模板渲染阶段失败。
- 隔离测试确认发言统计中的 speaker HTML 未被转义。
- 部署清单检查确认 `server/local.cjs` 和 `vendor/echarts.min.js` 未包含在现有打包及 Docker COPY 列表中。
- 线上只读检查确认首页、任务列表和设置可匿名访问，原始转写文件路径可返回成功状态。
- 未确认 GitHub 远端当前提交与本地提交完全一致；建议增加版本接口后再做部署一致性核对。

