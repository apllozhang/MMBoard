# MMBoard 最终验收复审报告

- 复审日期：2026-09-23
- 评审身份：独立评审方（只读检查，未修改产品源码、生产配置或业务数据）
- 交付说明：`MMBOARD_最终验收交付_2026-09-22.md`
- 功能基线：`6c9a255`
- 本地及 GitHub 当前 HEAD：`5be0d8c`（位于 `6c9a255` 之后的文档提交）
- 生产版本：`/api/version` 返回 `6c9a255`
- 最终结论：**暂不通过最终验收；发现 1 项 P1，未满足“无新增 P0/P1”停止标准。**

## 1. 结论与裁定

本轮交付中的正常路径整改大部分已由可复现的运行期断言证明，第四轮指出的 R05、R09、R15、R18、R19 问题均已有对应实现和验证证据。R13 的“分块原文实际进入 map prompt”也已成立，先前假阳性已消除。

但 R13 的异常路径仍会把不完整结果作为普通完成结果交付：某个 map 分块返回空正文、坏 JSON 或请求失败时，代码捕获异常并继续 reduce；该分块中的唯一决议会丢失，最终返回值没有被强制标记为 `partial`，也没有持久化一个不可被下游模型省略的结构化缺失标志。交付测试明确验证了“坏 JSON 时中段决议不进入结果”，却没有断言最终结果必须显示不完整警告。该行为违反最终验收目标中的“失败必须可见、不得把不完整纪要冒充完整纪要”，定级为 **P1**。

因此，本轮不能按交付文档第 5 节停止整改。建议只做一次小范围修复与针对性复核，不再扩展新功能或重开已通过项目。

## 2. 版本口径核验

| 检查项 | 结果 | 证据 |
|---|---|---|
| 本地代码树与功能提交一致 | PASS | `git diff --quiet 6c9a255..HEAD -- server src tools scripts deploy package.json package-lock.json` 返回一致 |
| GitHub HEAD | PASS（说明需更新） | `git ls-remote` 返回 `5be0d8c4e3ba4723c25fc9c132c3e030fe545430`；该提交晚于交付说明中提到的 `b25358c`，仍为文档提交 |
| 生产功能版本 | PASS | `http://10.20.30.203:8099/api/version` 返回 commit `6c9a255`，builtAt `2026-09-22T16:12:19.853Z` |
| 构建 | PASS | `npm run build` 成功，Vite 65 个模块完成构建 |
| 密钥扫描 | PASS | `npm run scan:secrets`：278 个跟踪文件，0 个泄漏命中 |

版本口径的实质关系成立：生产运行功能版本与 `6c9a255` 一致，当前仓库 HEAD 只增加文档。后续交付说明应把 GitHub HEAD 更新为 `5be0d8c`，避免继续声称 `b25358c` 是最新文档提交。

## 3. 交付测试独立复跑

| 命令 | 结果 | 独立复跑结论 |
|---|---:|---|
| `node scripts/verify_batch_a.cjs` | 167/167 | PASS，但其中 R13 异常路径的预期值存在验收口径缺陷，见 F01 |
| `python scripts/regression_batch1.py 8793` | 30/30 | PASS |
| `python tools/local-asr/test_server.py` | 11/11 | PASS；控制台中文乱码不影响退出码和断言，坏音频的 ffmpeg 错误为预期分支 |
| `node scripts/browser_matrix.mjs` | 13/13 | PASS |
| `node scripts/funasr_load_test.cjs` | 12/12 | PASS；真实服务可达，串行、坏音频、3 并发及结果隔离均通过 |
| `npm run build` | 成功 | PASS |

测试数量通过不等同于验收自动通过。测试必须断言正确的业务结果；F01 正是“脚本通过，但通过条件不完整”的实例。

## 4. 收尾项逐项复核

### R05：LLM/ASR 目标地址约束 — PASS

证据：

- `resolveLlmTarget` 保留 baseUrl 的 path/query，DNS 校验后使用已校验 IP 建连，并保留原始 Host/SNI。
- `/api/settings/test` 实际走 `resolveLlmTarget + postJson`；302 不自动跟随。
- `/api/settings/test-asr` 与真实本地 ASR 路径共用 `guardedFetch`，逐跳校验重定向。
- 运行期断言覆盖 DNS 劫持、精确 host:port 白名单、路径保留、外部重定向零命中和设置保存回读。

**对本地 ASR 保留私网 CIDR 的裁定：接受。** 本地 ASR 的产品用途是发现局域网工作机服务，采用私网 CIDR 与 LLM 精确白名单采用不同策略有业务依据；目前接口需要认证、只探测受控路径、逐跳限制在私网且不回传任意响应正文。建议在部署层继续限制容器可访问网段，但不把“改成精确列表”作为本次代码验收条件。

### R09：额度账本故障前置阻断 — PASS

证据：

- 真实讯飞且非 demo 路径在调用转写服务前执行 `assertQuotaReady()`。
- 主备额度文件双坏时，任务明确失败，讯飞 stub 请求数保持不变。
- 转写后记账失败会设置 `quotaRecordFailed` 并在步骤中提示人工核对。

说明：预检无法彻底消除“预检成功后磁盘瞬时故障”的竞态，但后置显式失败标记已覆盖该剩余风险，达到本项目验收目标。

### R13：超长会议分块提取 — PARTIAL / P1

正常路径证据：

- `mapChunk(chunk, ...)` 的 prompt 真实包含当前分块全文。
- 测试 stub 只在 prompt 真实出现标记词时返回标记，且有第一块不含第二块标记的否定断言。
- 2 块和 3 块会议的中段唯一决议均进入 reduce 和最终结果。

阻断问题见 F01：分块失败没有可靠传播为最终 `partial` 状态，关键内容可丢失但结果仍按完成交付。

### R15：行动项证据强绑定 — PASS

证据：

- 有效 `tref` 时只在与该时间范围重叠的转写片段内核验 quote。
- 覆盖起止颠倒、超音频边界、跨范围引用、假 quote、仅 quote 无 tref 等路径。
- 渲染层按失败类型显示可读警示，真实证据不误报。

### R18：上传资源治理 — PASS（附 P2 优化项）

证据：

- 上传占用、并发预留、队列满、假音频拒绝均有运行期断言。
- ffprobe 已改为异步队列，并发上限为 2；真实 wav 与不存在文件均有验证。

存在一个非阻断的保守计数问题，见 F02。它不会造成越过配额，但会提前拒绝接近配额的合法上传。

### R19：真实 FunASR 验证 — PASS

证据：

- 脚本现场生成两个可区分的语音夹具，无需仓库提交音频。
- 本次真实环境复跑为 12/12：健康检查、串行内容、坏音频失败、3 并发完成、内容对应和互不串扰全部成立。

### R23：浏览器矩阵 — PASS（NVDA 仍属正常迭代）

证据：

- Playwright 13/13，覆盖多层 dialog、inert/aria-hidden、焦点圈闭、键盘打开/关闭、进度条语义、320px 宽度和 420px 高度。
- 这些证据足以关闭自动化浏览器矩阵；NVDA 实机听读抽样仍按双方既定口径进入正常迭代。

## 5. 新发现

### F01 — P1：分块失败未强制标记最终纪要不完整

**位置：** `server/llm.cjs` 的 `analyzeChunked`；对应测试位于 `scripts/verify_batch_a.cjs` 的 R13 段。

**触发方式：** 超长会议进入 map/reduce；任何一个 map 请求返回空正文、坏 JSON、超时或其他异常。

**当前行为：**

1. `analyzeChunked` 捕获该块异常，把一段“第 N 块提取失败”的自然语言放入 `notes`，然后继续 reduce。
2. reduce 模型仍可返回结构完整的 JSON；`analyzeOnce` 只依据 reduce 输出缺字段或 stop reason 决定 `partial`。
3. map 失败状态没有作为结构化标志合并进最终返回值，所以最终结果可能 `partial=false`。
4. 当前坏 JSON 测试确认中段唯一决议已经丢失，却只断言“不伪造进入结果”，没有断言用户必须看到“不完整”警告。

**影响：** 用户会把遗漏关键决议的纪要当成完整结果使用。对会议纪要产品而言，这是比任务明确失败更危险的静默完整性错误。

**要求的修复：**

1. 记录 `failedChunkIndexes`，不要只把失败信息写进供 LLM 处理的自然语言。
2. 最终返回值强制设置 `partial=true`，并带结构化字段，例如 `chunkFailures: [{ index, reason }]`；模板必须显示不可被模型省略的醒目警告。
3. 每个 map 至少复用现有瞬态错误重试策略；确定性 4xx 或连续解析失败仍进入失败标记。
4. 增加以下失败用例，并先证明旧代码为红：
   - 标记所在块坏 JSON：决议可丢失，但 `partial=true` 且页面显示“第 2 块提取失败，纪要可能缺失”。
   - 非标记块空响应：即使关键决议仍存在，也必须 `partial=true`。
   - map 瞬态 503：重试后成功则完整；重试耗尽则 partial。
   - reduce 成功返回完整字段时，map 失败标志仍不得被覆盖。

**验收标准：** 以上用例通过；正常 2/3 块用例保持 `partial=false`；浏览器或模板断言确认警告实际可见。

### F02 — P2：上传 Content-Length 被重复计入配额投影

**位置：** `server/server.cjs` 上传前中间件与 `server/pipeline.cjs` 的 `quotaExceeded`。

**原因：** 请求先执行 `reserveUploadQuota(cl)`，使 `reservedUploadBytes` 已包含本次上传；随后又调用 `quotaExceeded(cl)`，其公式为“现有占用 + 全部预留 + incomingBytes”，因此本次 `cl` 被计算两次。

**影响：** 不会突破配额，但在接近 20GB 上限时会比设计值更早返回 503。单个 2GB 上传可能要求约 4GB 的剩余额度。

**建议：** 明确一种计数契约：先检查 `usage + existingReservations + cl`，成功后再预留；或预留后调用 `quotaExceeded(0)`。新增边界测试，验证 `usage=quota-cl` 时可接受、再多 1 字节时拒绝，并验证两个并发上传不会共同越界。

## 6. 后续工作边界

以下事项不阻断本次代码收尾，继续按正常产品迭代管理：

- 代理层请求体限制和代理层速率限制。
- 镜像层秘密扫描设为 CI 强制门禁。
- NVDA 实机听读抽样。
- 凭据轮换及留痕，由项目方按既定决策单独执行。
- 生产容器 CPU/内存限制、非 root 和 HTTPS，继续沿用项目方已书面接受的内网部署决策。

## 7. 下一轮最小复核范围与停止标准

开发方只需修复 F01；F02 可一并修复，也可登记为普通迭代。下一轮无需重做全部历史审查，提交以下证据即可：

1. 功能提交、GitHub HEAD、生产 `/api/version` 的版本关系。
2. R13 新增的四组故障注入断言及原有 2/3 块正常断言。
3. 模板或浏览器断言，证明 map 失败警告对用户实际可见。
4. `node scripts/verify_batch_a.cjs` 全量通过、`npm run build` 通过。
5. 若未改 ASR、浏览器交互及部署脚本，可不重复真实 FunASR 压测、30 项基础回归和完整浏览器矩阵；提供代码 diff 证明这些范围未变化。

当 F01 关闭且未引入新的 P0/P1，即满足最终停止标准，整改可结束，其余事项转正常产品迭代。
